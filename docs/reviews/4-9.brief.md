# Review brief — step 4-9, shared variable editor lifecycle

- **Repo:** `/Users/ccarpio/Developer/Utils/espansoConfig` (uncommitted working tree on `main`, base `e3cb1da`).
- **Phase and goal:** step 4-9 of Phase 4, defined in `docs/decisions/4-split-notes.md` §2 `### 4-9`
  (rulings in §3, esp. 21 and 22). Delivers `variableEditor.ts` and `variableInsertion.ts`: variable
  baseline and buffers, history, the compound *Insert* action (one undo step), whole-container reapply,
  retention, compare and copy, and ruling 21's recovery refusal; `draftWith` in `matchEditor.ts` now sends
  variables. The worker's record is `docs/decisions/4-9-notes.md`.
- **Changed files:** new `src/lib/browser/{variableEditor,variableInsertion}.ts`,
  `src/lib/browser/variableEditor.test.ts`, `docs/decisions/4-9-notes.md`; modified
  `crates/espansoconfig-core/src/{authoring.rs,model/match_view.rs,model/mod.rs}`,
  `crates/espansoconfig-core/tests/authoring_snapshots.rs`, `src-tauri/src/{wire_contract,dictionary_contract}.rs`,
  `src/lib/browser/{matchEditor,recovery,saveOutcome,workspace.svelte,fixtures}.ts`,
  `src/lib/browser/{recovery,workspace}.test.ts`, `src/lib/components/{MatchEditor,MatchCreator}.svelte`,
  `src/lib/i18n/{index.ts,en.json,es.json,dictionaries.test.ts}`, `src/lib/ipc/types.ts`.
  (`PROGRESS.json` is a workflow record — ignore.)
- **Verification run by the orchestrator, all exit 0:** `cargo test --workspace -- --test-threads=1`
  (1729 passed, 0 failed); clippy `-D warnings`; `cargo fmt --check`; `npm run check` (491 files, 0/0);
  `npm test` (4141); `npm run build` (216 modules); bundle oracle correct; core has no tauri dependency.
- **Risks to probe:**
  1. Reapply correctness: unchanged container reapplies; shifted/changed list collides; fully present intended
     result counts as satisfied — and whether the container fingerprint (value-only, 4-8 open item) is a sound unit.
  2. Every drafted scalar and structural variable action surviving both conflict origins (save conflict and
     external change); retention/compare/copy.
  3. Compound *Insert*: one undo step restoring both caret text and variable; `\r` refusal for any
     textarea/input-drafted text (CLAUDE.md §6).
  4. Consent: unbound findings transfer between candidates (4-8) — is consent discarded whenever the draft changes?
  5. Recovery refusal (`variablesNotCarried`): can recovery still create a snippet whose references lost
     their definitions by any path?
  6. R36/R37 via `BrowserState.variableStructureRead` — does any comment claim a guarantee TypeScript cannot force?
  7. The Rust projection change (`match_view.rs`): byte spans sliced in Rust, not JS; no preservation regression.
  8. `draftWith` sending `vars`/`var_intents`: an initially absent field left blank stays `'Unchanged'`;
     no regression for matches without variables.
  9. i18n: 33 keys per language via typed accessors, no hardcoded strings.
- **Known deviations (the worker's):** a variable reorder is offered and built into a request but nothing
  sends `moveVariable` yet (to 4-11); B1's variable-list regression is model-level, the mounted case owed
  to 4-11; one new variable per draft.
- **Review file:** `docs/reviews/4-9.md`.
- **Time budget:** 15 minutes.
