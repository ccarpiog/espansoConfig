NOT READY

1. **closed.** The production contract now represents the subject and positional anchor separately (`crates/espansoconfig-core/src/reconcile.rs:309`, `crates/espansoconfig-core/src/reconcile.rs:415`, `crates/espansoconfig-core/src/reconcile.rs:450`). `reconcile` resolves both with the same `fresh` argument in one call, and placement hard-codes `ExactItem` rather than accepting a confidence parameter (`crates/espansoconfig-core/src/reconcile.rs:562`, `crates/espansoconfig-core/src/reconcile.rs:594`). The move resolves its `after` identity once, captures the returned item beside the exact subject, and sends both through `OneSave` (`src-tauri/src/commands.rs:997`, `src-tauri/src/commands.rs:1009`, `src-tauri/src/commands.rs:1025`). Creation correctly uses the same placement slot (`src-tauri/src/commands.rs:1220`, `src-tauri/src/commands.rs:1227`); the other four commands select the required subject and empty-placement modes at `src-tauri/src/commands.rs:1104`, `src-tauri/src/commands.rs:1302`, `src-tauri/src/commands.rs:1369`, and `src-tauri/src/commands.rs:1453`. This is closed in production. The command-selection coverage gap introduced by the fix is reported below.

2. **closed.** `conflict_after_the_lock` remains the sole production constructor: it refreshes once, clones `disk_text`, calls `reconcile(request, fresh)`, clones `fresh.view`, and takes `disk_revision` from that same view (`src-tauri/src/commands.rs:1523`, `src-tauri/src/commands.rs:1530`, `src-tauri/src/commands.rs:1537`, `src-tauri/src/commands.rs:1539`). The new interleaving is discriminating. R1 duplicates the subject trigger and removes the placement's exact bytes, while R2 makes the trigger unique and restores the placement bytes (`src-tauri/src/commands.rs:3307`, `src-tauri/src/commands.rs:3318`); the test first proves `found` is R1, then proves the payload revision is R2, the subject contains R2-only text, and both returned identities carry `disk_revision` (`src-tauri/src/commands.rs:3367`, `src-tauri/src/commands.rs:3392`, `src-tauri/src/commands.rs:3402`, `src-tauri/src/commands.rs:3421`). Resolving against R1 would therefore return `AmbiguousTrigger` and `NoExactCorrespondence`, not the two `Identified` arms asserted for R2.

3. **closed.** Eligibility is independently stated as a parsed snapshot plus a projected match whose path ends in an index; it does not call `capture` or reuse an implementation refusal (`crates/espansoconfig-core/tests/reconcile.rs:826`, `crates/espansoconfig-core/tests/reconcile.rs:834`). Every eligible attempt must capture or panic with the fixture name, and eligible, captured, identified, and ambiguous outcomes are counted separately (`crates/espansoconfig-core/tests/reconcile.rs:854`, `crates/espansoconfig-core/tests/reconcile.rs:863`, `crates/espansoconfig-core/tests/reconcile.rs:909`). The synthetic sweep has an independent lower bound and a present real corpus must be non-zero (`crates/espansoconfig-core/tests/reconcile.rs:921`, `crates/espansoconfig-core/tests/reconcile.rs:939`). This predicate restates the public eligibility boundary, not `capture`'s refusal branches: an ownership-derivation or snapshot-identity regression in `capture` now panics rather than shrinking the audit.

4. **not closed.** The eight named corrections and the four reported follow-up corrections are present, but the semantic sweep left narrower instances of the same defect in production strings, rustdoc, and a test comment. In particular, `NoExactCorrespondence` means that no candidate has the same **owned-run digest** (`crates/espansoconfig-core/src/reconcile.rs:637`, `crates/espansoconfig-core/src/reconcile.rs:650`), yet both dictionaries still say the whole snippet is not written exactly the same (`src/lib/i18n/en.json:772`, `src/lib/i18n/es.json:772`). The existing comment-ownership case demonstrates the counterexample: the mapping is byte-identical while exact owned-run correspondence refuses (`crates/espansoconfig-core/tests/reconcile.rs:453`, `crates/espansoconfig-core/tests/reconcile.rs:484`). Also, the generic `Refused` sentences say identification/search happened “in the file as it is now” (`src/lib/i18n/en.json:761`, `src/lib/i18n/en.json:765`, `src/lib/i18n/es.json:761`, `src/lib/i18n/es.json:765`), although `NoAnchorInBase` is returned without consulting the fresh snapshot (`crates/espansoconfig-core/src/reconcile.rs:234`, `crates/espansoconfig-core/src/reconcile.rs:338`). The new placement rustdoc makes the same false fresh-snapshot claim (`crates/espansoconfig-core/src/reconcile.rs:442`), and the test comment “A snapshot that did not parse holds no sequence to search” repeats the old content claim (`crates/espansoconfig-core/tests/reconcile.rs:528`). Correct all of these as specified under the Medium finding below.

5. **closed.** The module test first proves that two identical candidates under one sequence are `AmbiguousExact`, then changes only one twin's public path head and requires the other to be identified (`crates/espansoconfig-core/src/reconcile.rs:920`, `crates/espansoconfig-core/src/reconcile.rs:924`, `crates/espansoconfig-core/src/reconcile.rs:941`, `crates/espansoconfig-core/src/reconcile.rs:946`). If `in_sequence` ignored the path head, the second assertion would remain ambiguous and fail. The acceptance case independently moves only the placement anchor's path and requires the subject to remain identified while placement refuses (`crates/espansoconfig-core/tests/reconcile.rs:755`, `crates/espansoconfig-core/tests/reconcile.rs:761`, `crates/espansoconfig-core/tests/reconcile.rs:768`). Both are falsifiable and reach the same-document sequence predicate.

6. **not closed.** The current `Record<string, never>` arms are counted as checked, and the reported totals are correct (`src-tauri/src/wire_contract.rs:2523`, `src-tauri/src/wire_contract.rs:2532`). However, `tagged_variant_fields` accepts any payload that merely **starts with** `Record<string, never>` (`src-tauri/src/wire_contract.rs:400`, `src-tauri/src/wire_contract.rs:405`). A TypeScript-only mutation such as `Record<string, never> | { readonly force: boolean }` would still be returned as an empty field set, keep `(122, 12, 71)`, and pass against Rust's `{}`. Thus the rewritten check still fails the finding's required mutation: an operand can be admitted on one side without failing. Require the payload after whitespace to be exactly `Record<string, never>` up to the enclosing variant boundary, and add parser tests rejecting a union, intersection, suffix, and `{}` while accepting the exact spelling.

## New findings introduced by the fixes

### Medium — the new placement/refusal prose claims a fresh-file search that may never occur

**Location:** `crates/espansoconfig-core/src/reconcile.rs:442`, `src/lib/i18n/en.json:761`, `src/lib/i18n/en.json:765`, `src/lib/i18n/es.json:761`, `src/lib/i18n/es.json:765`

`PlacementMode::Refused(NoAnchorInBase)` is decided from R0 and `reconcile_placement` returns it without reading `fresh` (`crates/espansoconfig-core/src/reconcile.rs:289`, `crates/espansoconfig-core/src/reconcile.rs:597`). Nevertheless the newly added placement rustdoc says, “The anchor could not be found again,” English says, “could not find ... in the file as it is now,” and Spanish says, “no pudo encontrar en el archivo tal como está ahora”. The older subject-level generic `Refused` sentence has the same narrower defect. These are false for base-capture failure, and misleading for `WrongDocument` and parse failure because no candidate search occurred.

Why it matters: these code sentences are intended for a later renderer, and the reason-specific `NoAnchorInBase` sentence correctly says the disk was not examined. Rendering both would contradict itself. The fix also records the false semantics as the enum contract, making a later consumer likely to repeat it.

Specific fix: use reason-neutral generic wording and leave the search detail to `ReapplyRefusal`.

- Corrected English subject: “espansoConfig could not establish correspondence for what this change was about.”
- Corrected Spanish subject: “espansoConfig no pudo establecer la correspondencia de aquello a lo que se refería este cambio.”
- Corrected English placement: “espansoConfig could not establish correspondence for the snippet this change was placed after.”
- Corrected Spanish placement: “espansoConfig no pudo establecer la correspondencia del fragmento tras el cual se colocaba este cambio.”
- Corrected placement rustdoc: “Correspondence for the named anchor could not be established, so no destination may be derived from this evidence.”

For the still-standing `NoExactCorrespondence` claim, use:

- Corrected English: “No snippet in that list carries the exact owned-line correspondence evidence recorded for this change. This operation or positional anchor requires exact owned-line correspondence, so nothing weaker will do.”
- Corrected Spanish: “Ningún fragmento de esa lista contiene los indicios exactos de correspondencia de las líneas propias registrados para este cambio. Esta operación o ancla de posición requiere una correspondencia exacta de las líneas propias, así que nada menos exacto sirve.”

Also change the test comment at `crates/espansoconfig-core/tests/reconcile.rs:528` to: “A snapshot that did not parse produces no parsed sequence to search.”

### Medium — the new tests bypass the production command-to-request mapping

**Location:** `src-tauri/src/commands.rs:1009`, `src-tauri/src/commands.rs:3304`, `src-tauri/src/commands.rs:3341`, `crates/espansoconfig-core/tests/reconcile.rs:93`

The command layer currently selects the right policies for all six writers, but no new test pins that complete mapping. The six placement acceptance cases build `ReapplyRequest` directly in the core test helper, and the provenance test also manually constructs its request. The only end-to-end move conflict sends `after: None` and therefore expects `NotAnchored` (`src-tauri/src/commands.rs:3067`, `src-tauri/src/commands.rs:3088`). Mutating the production move request at `src-tauri/src/commands.rs:1011` to always use `PlacementMode::NotAnchored` would leave every added or rewritten test green. Equivalent wrong-policy mutations in save, create, delete, and duplicate also lack a command-level conflict assertion.

Why it matters: 2c-4b-2 will trust this payload rather than reconstruct correspondence. A correct core plus a wrongly wired command gives a valid-looking but incomplete or over-permissive answer, exactly the class the High fix was meant to remove.

Specific fix: add command-level conflict tests that exercise and assert the actual request selected by each writer. At minimum, cover move-after and create-after through their public session methods and assert an `Identified` or specifically refused placement. Also pin save as the sole weak subject, move/delete/duplicate as exact subjects, creator as targetless, raw as unsupported, and every non-`after` operation as `NotAnchored`. The fixtures must make exact and trigger-only answers differ so a confidence mutation fails.

## Scope and standing constraints

The three declared deviations are sound. Rewording `targetless` follows from moving creation's `after` identity into `placement` (`src/lib/i18n/en.json:759`, `src-tauri/src/commands.rs:1221`). Returning creation's anchor in `placement` is a necessary refinement of Q3's single-resolution sketch and satisfies Q4's exact-anchor rule. Registering `ReapplyPlacement` as a third dictionary namespace without a renderer preserves the repository's code-to-string rule; the contract explicitly records that rendering waits for 2c-4b-3 (`src-tauri/src/dictionary_contract.rs:182`, `src-tauri/src/dictionary_contract.rs:191`).

The fixes remain evidence-only: the command module states that no path reads the answer and no written byte depends on it (`src-tauri/src/commands.rs:72`). The six writers still share `run_one_save`, whose only production call to `save_document` is at `src-tauri/src/commands.rs:935` and `src-tauri/src/commands.rs:959`; the command surface remains twelve commands, six writing (`src-tauri/src/commands.rs:3`, `src-tauri/src/commands.rs:19`). No choice, capability, renderer, `.svelte` behavior, force path, second lock, projection-to-YAML path, merge, index tie-break, cross-sequence move, mixed move batch, or Tauri dependency in the core was introduced. D2r and R25 remain stated at the command boundary (`src-tauri/src/commands.rs:31`, `src-tauri/src/commands.rs:38`), and the correspondence decision path never reads `item_index` (`crates/espansoconfig-core/src/reconcile.rs:148`, `crates/espansoconfig-core/src/reconcile.rs:613`).
