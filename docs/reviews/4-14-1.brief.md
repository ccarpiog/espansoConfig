# Review brief — step 4-14-1

- **Repo:** /Users/ccarpio/Developer/Utils/espansoConfig (review the **uncommitted** tree against HEAD `bebd7c2`).
- **Phase:** 4-14-1 — the seven kind submodels for a new variable (spec: `docs/decisions/4-split-notes.md`
  `### 4-14` and its 2026-09-26 cut addendum; notes `docs/decisions/4-14-1-notes.md`).
- **Goal:** Date, Random, Echo, Clipboard, Shell, Script and Match submodels for inserting a new variable:
  closed wire shapes, required-parameter readiness mirroring Rust `required_param`
  (`crates/espansoconfig-core/src/validate/mod.rs`; `date.format` is not required), warning values,
  unfamiliar text kept, `\r` refused at load/edit/send, participation in undo, conflict and recovery through
  the existing insertion lifecycle; a test that no command reads the clipboard or spawns a process for a
  variable.
- **Changed files:** new `src/lib/browser/variableKinds.ts` (+ test), `scripts/lint/no-execution.test.ts`,
  `docs/decisions/4-14-1-notes.md`; modified `src/lib/browser/variableGroup.ts` (+ test),
  `src/lib/components/VariableGroup.svelte`, `src/lib/components/MatchEditorVariables.test.ts`,
  `src/lib/i18n/{en,es}.json`, `src/lib/i18n/index.ts`, `src/lib/i18n/dictionaries.test.ts`. No Rust source.
  (`PROGRESS.json` and `4-split-notes.md` are workflow records.)
- **Verification run (all exit 0):** `cargo build --workspace`; `cargo test --workspace -- --test-threads=1`
  (1735 passed, 0 failed); `cargo clippy --workspace --all-targets -- -D warnings`; `cargo fmt --check`;
  `npm run check` (504 files, 0 errors/warnings); `npm test` (4391 passed); `npm run build` (224 modules);
  bundle oracle: server-only markers absent, client-only present.
- **Risks to probe:**
  1. Does each kind's emitted wire shape match what the Rust insertion path accepts and what espanso 2
     expects (param names, required params vs `required_param`)? Any kind silently dropping or rewriting
     unfamiliar text, or quoting/inferring a type (D2u)?
  2. `\r` refusal at all three points (load, edit, send), including a check-and-spend or property-read gap.
  3. Undo, conflict and recovery: does insertion of each kind really go through the existing
     `addVariable`/`insertVariable` lifecycle, or is any of it copied? Blank field → "not given" semantics
     (the echo form now refuses empty text — is that a regression for any existing caller?).
  4. The no-execution scan: is it vacuous (patterns that could never match, a scope that misses
     `src-tauri/`, core, `src/lib/ipc/`), and is the pinned `mkfifo` exception tight?
  5. i18n: every new user-facing string through the dictionaries in both languages; no hardcoded text;
     Shell/Script warnings claim nothing the app does not guarantee (CLAUDE.md §5: a comment or sentence
     claiming a guarantee the code does not give is the worst defect class).
  6. Any claim in the notes not backed by the tree.
- **Review file:** `docs/reviews/4-14-1.md`.
- **Time budget:** 15 minutes.
