READINESS: NOT READY

### Instrument defects

1. Medium — `src-tauri/src/probe.rs:215,251-275,294-310,337-356,391-420`, **Claimed:** the module and function contracts at `src-tauri/src/probe.rs:40-46,235-239,359-367` say that `create_new` closes the temporary-file race, `rename` safely replaces the target's final component, and only an ancestor-directory swap remains unproven. **Actually true:** every call of both IPC writers does pass through `replace_the_target` (`src-tauri/src/probe.rs:465-489,496-507`), and `confined_target` really does require the canonical target, at the instant of the check, to have exactly `<launch>/xdg/espanso/match/conflict.yml` beneath the canonical launches root (`src-tauri/src/probe.rs:251-275`). `create_new` also genuinely refuses a symlink that exists when `open` runs (`src-tauri/src/probe.rs:397-406`). It does not, however, bind the later rename to the file that was opened: after writing and syncing, the handle is dropped and `rename(temporary, target)` resolves the temporary pathname again (`src-tauri/src/probe.rs:407-420`). A racer can unlink or rename the opened temporary and plant another file or symlink at that name before line 419; the writer then installs the replacement entry, not the bytes it wrote. The source has the same check-and-spend shape: it is canonicalized and checked at lines 294-310, then reopened by pathname at lines 391-396, so a final-component source swap can make outside source bytes be installed. C07 covers only a symlink already present when `create_new` runs, not either later re-resolution. This instrument is temporary, operator-controlled, never shipped, and deleted at 2c-5-7, which mitigates severity but does not make either guarantee true. **Minimal fix:** either use OS-specific descriptor-relative primitives that bind validation, creation and installation to pinned objects, or classify the source-final-component and post-`create_new` temporary-name swaps as residual and unproven alongside the ancestor swap; do not call Arm A fully closed.

### Prose defects

1. Medium — `docs/decisions/2c-5-5a-instrument-rebuild.md:879-895,986-1003,1100-1131`, **Claimed:** §8.1 tells 5b that there is “no second pathname resolution,” §9.1 calls the no-shell/O_EXCL part closed by construction, and §10.1 says Arm A is fixed properly while Arm B is the open case. **Actually true:** Arm B's disclosure is honest on its own terms: §4.5 (`:513-522`), §8.1 (`:887-892`) and §10.1 (`:1122-1131`) all plainly say the ancestor swap is unconstructed, unproven and not closed, and they identify the temporary/non-shipping context as acceptance rather than proof. It is nevertheless presented as the only residual. The code still resolves the checked source at `std::fs::read` and resolves the created temporary again at `rename` (`src-tauri/src/probe.rs:391-420`). Thus §8.1's “no second pathname resolution” at `:883-884`, §9.1's construction closure at `:995-998`, and §10.1's full Arm-A closure at `:1102-1114` overstate what the code forces. **Minimal fix:** make all three sections say in the same confinement sentence that exact target shape and pre-existing temporary exclusion are forced, while source-final-component rebinding, post-open temporary rebinding, and ancestor-directory rebinding are not; tell 5b those same limits.

2. Medium — `docs/decisions/2c-5-5a-instrument-rebuild.md:986-1003`, **Claimed:** §9.1's first bucket is “Closed and measured,” including “A plan is required.” **Actually true:** §4.3 explicitly says no launch exercises a writer's no-plan refusal (`docs/decisions/2c-5-5a-instrument-rebuild.md:430-435`). C05/C06/C09/C10 measure second-writer pathname refusals, not the plan gate; §4.4 also correctly says they do not separately measure the third writer (`:472-476`). The plan gate and the third writer's shared confinement are source properties, not measurements. **Minimal fix:** split the first bucket once more, or label each item individually: plan gating and reuse by the third writer are closed by source construction; only the listed second-writer refusals are measured.

3. Low — `docs/decisions/2c-5-5a-instrument-rebuild.md:674-690,1156-1180`, **Claimed:** §10.4 says §6.1 states the general rule covering all five swept instances. **Actually true:** §6.1's rule covers the temporary-file absence in §9.2 and the unchanged-decoy readings in §§4.4-4.5: final state cannot distinguish no write from an identical or transient write. It does not cover the other two §1 instances that §10.4 lists: present absence cannot establish when the old tree disappeared (`:91-96`), and a string search over selected files cannot establish that no retained artifact contains private material (`:98-105`). Those two instances are individually narrowed correctly, but they are chronology/scope limits, not cases of §6.1's no-write equivalence. **Minimal fix:** extend §6.1 with a real general negative-evidence rule—an absence observation is bounded to its stated time, corpus and predicate and proves no broader history or scope—or stop claiming that one rule governs all five.

4. Low — `docs/decisions/2c-5-5a-instrument-rebuild.md:62-64`, **Claimed:** the tree inventory says 74 of the round-1 manifest's 78 entries still verify. **Actually true:** §5.9 says 75 of 78 verify and names exactly three failures (`:578-588`), and §10.5 repeats the measured 75-of-78 result and the same three failures (`:1182-1189`). The inventory contradicts both dispositions. **Minimal fix:** change `74` to `75` at line 63.

### Round-2 finding status

1. PARTIALLY CLOSED — the exact target-shape check is real and `create_new` closes the pre-open symlink insertion tested by C07, but source reopening and post-open temporary-path rebinding remain, while only the ancestor form is disclosed.
2. PARTIALLY CLOSED — the record now has measured/construction/open buckets and candidly leaves Arm B open, but it puts an unmeasured plan gate in the measured bucket and over-claims Arm A's construction closure.
3. CLOSED — `render_probe` returns both I/O failures (`src-tauri/src/probe.rs:164-170`), and the Rust, TypeScript and §9.5/§10.3 prose consistently say that `--- failed` is only attempted and a transcript may be silently truncated (`src-tauri/src/probe.rs:146-154`; `src/probe.ts:113-122,1002-1018`; decision record `:1037-1045,1149-1154`).
4. PARTIALLY CLOSED — P01 is corrected exactly at §6.3 (`docs/decisions/2c-5-5a-instrument-rebuild.md:705-724`) and all five swept sites are individually narrowed, but §6.1 does not actually generalize over the two §1 chronology/scope instances that §10.4 says it covers.

### Did the fix round create anything?

Yes. The new `create_new` → write/sync → drop handle → pathname `rename` sequence at `src-tauri/src/probe.rs:397-420` creates a post-open check-and-spend gap: exclusivity was obtained for one opened file, but the consuming install spends a name that can have been rebound. The round also created the false “no second pathname resolution” inheritance claim in §8.1 (`docs/decisions/2c-5-5a-instrument-rebuild.md:879-895`) and the 74/75 manifest-count contradiction (`:62-64,578-588,1182-1189`). I swept the corresponding shapes: both writer entry points share `replace_the_target`; `write_all` and `sync_all` are consumed, the cleanup results are deliberately discarded and disclosed (`src-tauri/src/probe.rs:407-426`), both frontend writer invokes are awaited (`src/probe.ts:639-655`), and the surviving source reopen at `src-tauri/src/probe.rs:391-396` is a narrower old TOCTOU rather than one newly created by this round.
