Reviewer: codex adversarial review

Codex adversarial review of the working tree at /Users/ccarpio/Developer/Utils/espansoConfig.
Codex verdict: needs-attention. 1 finding(s): 0 blocker(s), 1 should-fix.

Do not ship yet: pending list text can survive a reparse and be saved into a different variable.

- [SHOULD-FIX] medium · confidence 1 — src/lib/components/VariableGroup.svelte:194 — Pending list text follows positional indices across baseline replacements
    pendingItems survives baseline replacement and is keyed only by variable/parameter positions. Reproduced through the mounted MatchEditor: enter new items for beta at index 1, move…
    Fix: Bind pendingItems to session.baseline.variables and clear or refuse stale entries when that baseline changes. Add a mounted regression cover…

Scope: the working tree only — anything already committed for this phase was outside it.

VERDICT: ship-with-fixes
BLOCKERS: none
SHOULD-FIX: src/lib/components/VariableGroup.svelte:194 — Pending list text follows positional indices across baseline replacements — Bind pendingItems to session.baseline.variables and clear or refuse stale entries when that baseline changes. Add a mounted regression cover…
NOT-VERIFIED: Fix pending-input ownership across reparses and verify that old text cannot reach another variable’s save.
