# Review brief — Phase 2d-6-8b (raw and restore panels' external-conflict rendering)

- **Repo:** /Users/ccarpio/Developer/Utils/espansoConfig — review the **uncommitted** tree against `HEAD` (`e8f42ce`).
- **Ignore entirely:** `src-tauri/src/main.rs`, `src/main.ts`, `src-tauri/src/probe.rs`, `src/probe.ts` (the temporary window-reading instrument, deliberately uncommitted) and `PROGRESS.json`.
- **Phase goal:** the second third of 2d-6-8 (`docs/decisions/2d-6-split-notes.md` §2, `### 2d-6-8` and the cut beneath it). On `RawEditor.svelte` and `RestorePane.svelte`: the conflict origin and observed revision, the comparison beside the disk text, `externalMessages`/`externalNotices`, the distinct reload effects (raw **reseeds** the draft, restore **retargets** keeping the candidate), the viewer refreshing through the guarded path while the editor conflicts, the restore candidate surviving, CR disclosure; direct submission refused while conflicted; supersession withdrawing the warning; bilingual mounted tests of every offered control. It also decides `2d-6-5-notes.md` §4 item 12 (a candidate dropped under a standing conflict). Binding rulings: `2d-6-split-notes.md` §3 entries 1, 10, 23, 32, 34-36, 38. Rendering pattern copied from `docs/decisions/2d-6-7b-notes.md`.
- **Record:** `docs/decisions/2d-6-8b-notes.md` (new) — check that every claim it makes is true of the code, especially what TypeScript forces.
- **Changed files:** `src/lib/components/{RawEditor,RestorePane}.svelte`; `src/lib/browser/{restore,rawEditor,saveOutcome,observationDelivery}.ts`; `src/lib/i18n/{en.json,es.json,index.ts}`; tests `src/lib/browser/restore.test.ts`, `src/lib/i18n/restoreCodes.test.ts`, `src/lib/components/{RawEditor,RestorePane,DetailPane}.test.ts`.
- **Verification run (orchestrator, each exit 0):** `npm test` 3156 passed (65 files); `npm run check` 449 files, 0 errors, 0 warnings; `npm run build` 193 modules, server-only markers absent, client-only present. Worker: `cargo test --workspace -- --test-threads=1` 1323 passed, clippy `-D warnings` and `cargo fmt --check` exit 0. No Rust changed.
- **Risks to probe:** (1) a committed write reported as an error, or any path that installs a disk version without `adoptDiskVersion` / a confirmed reload; (2) the raw editor accepting or reconstructing a `\r` (CLAUDE.md §6) — reload from a CR-bearing disk text must refuse, and disk text with `\r` must never go into a textarea/input; (3) a conflicted surface still able to issue a save command (forced press); (4) the restore retarget losing or silently changing the candidate, and the item-12 decision drawing a "kept" sentence that is false; (5) reads of session/projection state after the last `current()` in any changed model transition (check-and-spend non-atomicity, CLAUDE.md §6); (6) hardcoded user-facing strings, hand-built i18n keys, EN/ES drift; (7) mounted tests that pass without exercising the claim (e.g. assert on a model value, not the drawn text).
- **Review file:** `docs/reviews/phase-2d-6-8b.md`
- **Time budget:** 15 minutes.
