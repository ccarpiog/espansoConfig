Reviewer: codex adversarial review

Codex adversarial review of the working tree at /Users/ccarpio/Developer/Utils/espansoConfig.
Codex verdict: needs-attention. 6 finding(s): 2 blocker(s), 4 should-fix.

ship-with-fixes — 5 source correctness blockers and 1 record-only finding. Reviewed the scoped ee78429 fix read-only; no files changed and no tests or builds run.

- [BLOCKER] high · confidence 0.99 — src/lib/browser/workspace.svelte.ts:3455 — Recheck the open generation after normalizing the projection
    Source defect. open() checks its generation at 3455, then the new ownedProjectionOf call executes command-controlled getters. On the final document, a getter can synchronously call…
    Fix: Normalize before the generation comparison and recheck immediately before publishing the completed open. Cover a final-document getter that…
- [BLOCKER] high · confidence 0.99 — src/lib/browser/workspace.svelte.ts:498 — Copy match identity fields before selection-follow guards
    Source defect. ownedMatchOf retains the command's MatchId object at line 500. The reader enumeration misses positionInSameParse → positionOf, whose selection.ts:134 predicate reads…
    Fix: Materialize MatchId fields before guards, audit the other identity inputs, and revalidate selection intent after lookup immediately before a…
- [SHOULD-FIX] medium · confidence 0.98 — src/lib/browser/workspace.svelte.ts:2963 — Fence explicit rereads against newer status observations
    Source defect. Start an explicit reread and hold its successful response; then accept a newer Unreadable observation for that document. applyUnreadable at 1081–1084 advances the ac…
    Fix: Capture per-document status or observation ownership before the read and reject an obsolete installation/clear when it changes. Cover a defe…
- [SHOULD-FIX] medium · confidence 0.98 — src/lib/browser/workspace.svelte.ts:2339 — Move the row lookup before the final failure-write checks
    Source defect. The failure callback compares openGeneration and statusWriteOf, but then reads held.id inside documents.some before writing stale. documents still retains listDocume…
    Fix: Evaluate membership before the final generation/token comparisons, leaving only owned-data operations before the write, or normalize summari…
- [SHOULD-FIX] medium · confidence 0.98 — src/lib/browser/observationTransitions.ts:968 — Recheck observation ownership after host callbacks
    Source defect. Reaching the registry arm does not prove that the earlier isNewest answer still holds. Between that check and this write, tellTheSurfaceAbout invokes openWriteSurfac…
    Fix: Recheck ownership after host reads and immediately before both stale writes; also revalidate the successful guard outcome. Replace the posit…
- [SHOULD-FIX] medium · confidence 0.99 — docs/decisions/2d-5-4-A-notes.md:172 — Correct the claimed number of discriminating cases
    Record-only defect. These lines claim nine cases each failed against pre-fix code, but the table enumerates eight: seven rows, with the epoch row naming two cases. The commit adds…
    Fix: Distinguish nine added tests, one rewritten test, eight recorded discriminating cases, and two non-discriminating additions. Preserve only s…

Scope: the working tree only — anything already committed for this phase was outside it.

VERDICT: ship-with-fixes
BLOCKERS: src/lib/browser/workspace.svelte.ts:3455 — Recheck the open generation after normalizing the projection — Normalize before the generation comparison and recheck immediately before publishing the completed open. Cover a final-document getter that…
BLOCKERS: src/lib/browser/workspace.svelte.ts:498 — Copy match identity fields before selection-follow guards — Materialize MatchId fields before guards, audit the other identity inputs, and revalidate selection intent after lookup immediately before a…
SHOULD-FIX: src/lib/browser/workspace.svelte.ts:2963 — Fence explicit rereads against newer status observations — Capture per-document status or observation ownership before the read and reject an obsolete installation/clear when it changes. Cover a defe…
SHOULD-FIX: src/lib/browser/workspace.svelte.ts:2339 — Move the row lookup before the final failure-write checks — Evaluate membership before the final generation/token comparisons, leaving only owned-data operations before the write, or normalize summari…
SHOULD-FIX: src/lib/browser/observationTransitions.ts:968 — Recheck observation ownership after host callbacks — Recheck ownership after host reads and immediately before both stale writes; also revalidate the successful guard outcome. Replace the posit…
SHOULD-FIX: docs/decisions/2d-5-4-A-notes.md:172 — Correct the claimed number of discriminating cases — Distinguish nine added tests, one rewritten test, eight recorded discriminating cases, and two non-discriminating additions. Preserve only s…
NOT-VERIFIED: Fix the five source correctness gaps and add focused getter/deferred-response coverage.
NOT-VERIFIED: Correct the evidence count and the ownership/depth claims in source and decision records.
NOT-VERIFIED: Commission the source-fix review required by CLAUDE.md §7.1.
