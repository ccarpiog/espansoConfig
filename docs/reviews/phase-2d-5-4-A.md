Reviewer: codex adversarial review

Codex adversarial review of the working tree at /Users/ccarpio/Developer/Utils/espansoConfig.
Codex verdict: needs-attention. 6 finding(s): 2 blocker(s), 4 should-fix.

do-not-ship — 5 source correctness blockers (2 high, 3 medium), plus 1 record-only finding. Reviewed the committed fix regions at 81e54db. No files changed; no builds or tests run.

- [BLOCKER] high · confidence 0.99 — src/lib/browser/workspace.svelte.ts:2694 — Cached projection getters still separate the guard from installation
    Source defect: copying fresh.value does not make installView atomic. open() retains injected getDocument values directly (3189), and installView reads those objects through views.f…
    Fix: Materialize the replacement's cached-view operands before the final guard, or normalize retained projections at ingress. Commit using owned…
- [BLOCKER] high · confidence 1 — src/lib/browser/workspace.svelte.ts:2644 — replaceSelection does not protect repair from nested re-entry
    Source defect, also repeated in notes §3.3 and §7 item 12: the claimed protection does not exist. repairAfter calls reresolve at 4580, which reads view.matches[position], source_te…
    Fix: Materialize repair operands before committing, and validate workspace, projection and selection generations after every caller-controlled re…
- [SHOULD-FIX] medium · confidence 1 — src/lib/browser/workspace.svelte.ts:2127 — An obsolete reread failure overwrites newer document status
    Source defect: defer a Changed reread, accept a newer Removed observation, then resolve the earlier read with a failure. Removal drops the projection and records removed, but the f…
    Fix: Fence failure-status mutations by the originating open generation, reread/projection generations and accepted observation sequence. Continue…
- [SHOULD-FIX] medium · confidence 1 — src/lib/browser/observationTransitions.ts:908 — The stopped-session arm erases a newer unreadable reason
    Source defect: start a deferred Changed/Projected reread for A while a surface covers B. Accept a newer Unreadable observation for A, recording unavailable with its typed reason, t…
    Fix: Check observation ownership before any refusal writes status. Keep the stopped-session check ahead of surface callbacks, but preserve newer…
- [SHOULD-FIX] medium · confidence 1 — src/lib/browser/workspace.svelte.ts:2119 — Successful explicit recovery leaves the new stale mark behind
    Source defect: after the guarded reread fails, call state.rereadDocument(document) and let it successfully install the current projection. That method supplies ALWAYS_PERMITTED (33…
    Fix: Give successful current installations an explicit, ownership-checked status transition, including manual rereads. Distinguish installed, sup…
- [SHOULD-FIX] medium · confidence 1 — docs/decisions/2d-5-4-notes.md:317 — The corrected command-reachability claim still omits document_text
    Record-only finding: §3.5 still says reload_document is the only document command applyObservation can reach. With the raw viewer open, the actual path is applyObservation → applyC…
    Fix: Distinguish the reload directly requested by observation arbitration from subsequent viewer reads. Document document_text reachability and p…

Scope: the working tree only — anything already committed for this phase was outside it.

VERDICT: ship-with-fixes
BLOCKERS: src/lib/browser/workspace.svelte.ts:2694 — Cached projection getters still separate the guard from installation — Materialize the replacement's cached-view operands before the final guard, or normalize retained projections at ingress. Commit using owned…
BLOCKERS: src/lib/browser/workspace.svelte.ts:2644 — replaceSelection does not protect repair from nested re-entry — Materialize repair operands before committing, and validate workspace, projection and selection generations after every caller-controlled re…
SHOULD-FIX: src/lib/browser/workspace.svelte.ts:2127 — An obsolete reread failure overwrites newer document status — Fence failure-status mutations by the originating open generation, reread/projection generations and accepted observation sequence. Continue…
SHOULD-FIX: src/lib/browser/observationTransitions.ts:908 — The stopped-session arm erases a newer unreadable reason — Check observation ownership before any refusal writes status. Keep the stopped-session check ahead of surface callbacks, but preserve newer…
SHOULD-FIX: src/lib/browser/workspace.svelte.ts:2119 — Successful explicit recovery leaves the new stale mark behind — Give successful current installations an explicit, ownership-checked status transition, including manual rereads. Distinguish installed, sup…
SHOULD-FIX: docs/decisions/2d-5-4-notes.md:317 — The corrected command-reachability claim still omits document_text — Distinguish the reload directly requested by observation arbitration from subsequent viewer reads. Document document_text reachability and p…
NOT-VERIFIED: Fix the installation, selection-repair and status-ownership defects; correct the associated source and record guarantees.
NOT-VERIFIED: Add focused regressions for the interleavings described above; execution was intentionally omitted from this review.
NOT-VERIFIED: Apply CLAUDE.md §7.1 to the resulting fix diff: source changes commission another scoped review.
