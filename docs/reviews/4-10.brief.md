# Review brief — step 4-10 (form editor model)

- **Repo:** /Users/ccarpio/Developer/Utils/espansoConfig (review the **uncommitted** tree against HEAD `2475308`).
- **Phase and goal:** step 4-10 of Phase 4, defined in `docs/decisions/4-split-notes.md` §2 `### 4-10`. Delivers `src/lib/browser/formEditor.ts`: one form model with shorthand and verbose adapters, the explicit *Add field* compound action, definition-only rows; the shorthand layout keeps the existing `form` content buffer. Acceptance: a layout edit creates or deletes no definition; *Add field* changes both parts and undo restores both; repeated placeholders share one row; an absent field left blank is `'Unchanged'`; a layout holding `\r` is refused at load, edit and send (and a CRLF file's untouched bytes are checked separately); every new value has a lifecycle disposition.
- **Changed files:** new `src/lib/browser/formEditor.ts`, `src/lib/browser/formEditor.test.ts`, `docs/decisions/4-10-notes.md`; modified `src/lib/browser/{matchEditor,recovery,saveOutcome,variableInsertion}.ts`, `src/lib/ipc/types.ts` (doc sentence), `src/lib/i18n/{en,es}.json`, `src/lib/i18n/index.ts`, `crates/espansoconfig-core/tests/form_definitions.rs` (one CRLF byte-exact test). `PROGRESS.json` is a workflow record, not a deliverable.
- **Verification run (orchestrator, all exit 0):** `cargo test --workspace -- --test-threads=1` 1734 passed; clippy `-D warnings`; `cargo fmt --check`; `npm run check` 493 files 0/0; `npm test` 4189 passed; `npm run build` 217 modules; bundle oracle correct; core has no tauri dependency. Failing-first evidence by 14 mutations in `/private/tmp/4-10/`.
- **Risks to probe:**
  1. `matchEditor.ts` grew by ~500 lines: does the form wiring break any existing draft/save/reapply/conflict/recovery path for matches without forms, or for variables (4-9)?
  2. Do the `\r` refusals really hold at load, edit and send (CLAUDE.md §6: textarea/input normalization)?
  3. Is an initially absent field left blank `'Unchanged'` (never `Set("")`)?
  4. *Add field* as one undo step; undo restores both layout and definition. Can a layout edit ever create or delete a definition?
  5. Reapply: the worker states a form never gets a "satisfied" verdict (a result already on disk collides). Is that safe and correctly documented?
  6. The placeholder parser is a TypeScript copy of the Rust one (`analysis/placeholder.rs`); parity is pinned only on samples. Any divergence that causes a wrong row?
  7. Any comment or record claiming a guarantee the code does not give (this project's worst defect class); any hardcoded user-facing string; i18n EN/ES parity.
- **Review file:** `docs/reviews/4-10.md`.
- **Time budget:** 15 minutes.
