1. HIGH — [workspace.svelte.ts:2322](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2322), [workspace.svelte.ts:2373](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2373), [workspace.svelte.ts:2568](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2568), [workspace.svelte.ts:2570](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2570), [2c-3c-2-notes.md:137](/Users/ccarpio/Developer/espansoConfig/docs/decisions/2c-3c-2-notes.md:137)  
   Concrete failure: the wrapper does not capture the selection which initiated the duplicate, or its `selectGeneration`, before awaiting the command. After the answer, it passes only the source identity to `adoptTheDocumentOnDisk`; that helper installs the fresh projection and asks whether the **current** selection equals the source. Start a duplicate while another snippet is selected, then select the source before the command answers; or start on the source, move to another snippet, and return to the source before the answer. In either history the current comparison succeeds and the adoption selects `moved`, dragging the person to the clone even though the source was not an unchanged initiating selection. This violates Q6/Q8's “never reclaimed after the person moves it” rule and falsifies the decision record's claim that the held source is the selection “that initiated the operation.” The two-document test at [workspace.test.ts:2949](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.test.ts:2949) exercises documents 2 and 3 genuinely, but only the one-way source-to-other-file history, so it cannot detect leave-and-return or other-to-source reclamation.  
   Smallest fix: capture the initiating `SelectedMatch` and/or `selectGeneration` synchronously before `commands.duplicateMatch`, require that capture to be the source, and require the selection intent to remain unchanged before following `moved`; otherwise run ordinary repair. Add both other-to-source and source-to-other-to-source deferred-command tests.

2. MEDIUM — [commands.rs:1353](/Users/ccarpio/Developer/espansoConfig/src-tauri/src/commands.rs:1353), [commands.rs:1364](/Users/ccarpio/Developer/espansoConfig/src-tauri/src/commands.rs:1364), [commands.ts:717](/Users/ccarpio/Developer/espansoConfig/src/lib/ipc/commands.ts:717), [matchDuplication.ts:365](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/matchDuplication.ts:365), [en.json:342](/Users/ccarpio/Developer/espansoConfig/src/lib/i18n/en.json:342), [es.json:342](/Users/ccarpio/Developer/espansoConfig/src/lib/i18n/es.json:342)  
   Concrete failure: `after_a_save` returns a committed `Saved` with `moved: None` not only when the fresh revision disagrees, but also whenever `workspace.refresh` fails; it can also return `None` when the fresh same-revision projection contains no match at the requested path. The new TypeScript JSDocs say that `null` on a commit means the file changed again, and both new dictionary sentences state that as fact. A transient post-commit read failure therefore produces a committed duplicate whose later browser re-read may succeed at the transaction's own revision, yet the model and future screen attribute the missing identity to a second file change that never happened. This is exactly the false-certainty class the record says the new copy avoids.  
   Smallest fix: describe `moved: null` only as failure to identify the clone, with a non-exhaustive set of possible causes, and add a boundary/model case where the command's post-save refresh fails without a second writer.

3. LOW — [dispatch_check.rs:886](/Users/ccarpio/Developer/espansoConfig/src-tauri/src/dispatch_check.rs:886), [commands.rs:1419](/Users/ccarpio/Developer/espansoConfig/src-tauri/src/commands.rs:1419), [workspace/mod.rs:503](/Users/ccarpio/Developer/espansoConfig/crates/espansoconfig-core/src/workspace/mod.rs:503), [2c-3c-2-notes.md:193](/Users/ccarpio/Developer/espansoConfig/docs/decisions/2c-3c-2-notes.md:193)  
   Concrete failure: the dispatch test says it proves the duplicated bytes are “on the disk,” but its oracle invokes `document_text`. That command explicitly serves `Workspace::document_text`, which returns the already-loaded `SourceDocument.source` cache and performs no refresh when the entry is loaded. The test therefore proves the dispatcher can read the session's post-save cached source, not independently that the target file contains those bytes; the decision record repeats the stronger disk claim. The direct command test does read the filesystem, so this does not remove all disk evidence, but this new dispatch assertion and its recorded guarantee are overstated.  
   Smallest fix: retain the IPC cache assertion for the boundary fact, and separately read the target through `std::fs` using the test directory before claiming disk persistence.

4. LOW — [notices.ts:70](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/notices.ts:70), [notices.ts:85](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/notices.ts:85)  
   Concrete failure: the JSDoc for `RepairAttribution` says the type identifies who “reordered the file,” while the same type now includes `requestedDuplicate`; the following member documentation correctly says a duplicate grows the file. A duplicate inserts without reordering the existing items, so the public contract contradicts itself at the exact insertion-versus-reorder distinction this change introduced separate notices to preserve. The new English and Spanish notice sentences themselves correctly say “grew”/“ha hecho crecer” and do not call the insertion a move.  
   Smallest fix: describe the type as attributing who changed the file, leaving the member-specific move/reorder and duplicate/growth claims below it.

## Verified without another finding

- The production refusal chain at `matchDuplication.ts:516-536` is exactly `mayHaveWritten`, `alreadyDuplicated`, `saveInFlight`, `conflict`, `outOfDate`, `notDuplicable`. The five adjacent-pair tests at `matchDuplication.test.ts:633-705` exercise every adjacency, including both arrival orders for the uncertain-send/commit pair; no precedence gap was found.
- Every actual write to `selected` still has its `selectGeneration` bump in the same synchronous block: `replaceSelection`, `open`, and `select` remain the only three spellings. `installView` and `forgetTheReplacedDocument` independently bump the affected document's `projectionGenerations`. The new duplicate adoption installs before it selects or repairs, and the lookup-in-flight test exercises that projection counter. Finding 1 is the separate missing initiating-intent capture, not a collapse of the two counters.
- A `committed: true` result remains in the `answered` arm. A failed browser re-read returns failed adoption beside the saved result, `forgetTheReplacedDocument` removes the stale projection and selection, and the model renders `fileWritten` plus `windowOutOfStep`. The cautious `may_have_written` path makes no duplicate attribution. No path in the production `CommandResult`/typed re-read flow converts the known commit into a failed save.
- The acknowledgement round trip preserves the candidate finding value. The Rust dispatcher sends the received finding array back unchanged; the browser model derives consent only from the refusal, snapshots that exact acknowledgement, and `submissionOf` reuses it rather than synthesizing a `ContentRevision`. The operand is the finding's candidate revision, not the request base.
- The live-projection gates are non-vacuous: `sessionIsLive` resolves through the supplied current views, and `beginDuplicate` compares its external `projected` identity against both frozen identities. The stale tests feed a separately minted `reread()` revision rather than comparing a fixture with itself. The component still owes the synchronous producer in step 3, as the record admits.
- `duplicated`, `invalidated`, and `mayHaveWritten` are only or-ed/set true. A committed save, any owed adoption, conflict, uncertain send, and failed recovery spend the session; `dismissDuplicationOutcome` clears presentation fields but none of those facts. No reset path un-spends them.
- The contract retabulation is real: the frontend declares twelve workspace commands; Rust registers those twelve plus the menu command; six are writers; `CommandError` has seventeen variants and the operand/serializer/dictionary walks include `duplicateSourceNotASequenceItem`. Both languages contain the new command-error sentence, and the Spanish duplicate namespace consistently uses `fragmento`, never `atajo`.
- The new notice sentences attribute growth rather than reorder, and the uncertain-send sentences preserve uncertainty. Apart from findings 2 and 4, I found no new English, Spanish, JSDoc, or decision-record guarantee contradicted by the code.
- The new model/wrapper tests use distinct pre/post revisions and node identities where staleness matters; the acknowledgement test echoes the actual refusal findings; the conflict test installs its supplied disk projection; and the two-document race really selects document 3 while document 2 commits. Apart from finding 3's cache-as-disk claim and finding 1's missing histories, no vacuous new assertion was found.
- The command uses `DuplicateItem::resulting_path()` and passes it as `at` to `run_one_save`; registration, same-sequence immediately-after placement, fresh clone identity, command-error cascade, and `requestedDuplicate` notice routing otherwise match Q5/Q6/Q8. No core file is modified by this working-tree phase.

## Checks run

- `git diff --check`
- `cargo test --workspace` — 1,045 passed, 0 failed
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo fmt --check`
- `npm test -- --run` — 1,297 passed, 0 failed
- `npm run check` — 0 errors, 0 warnings
- `npm run build` — 169 modules

READINESS: NOT READY

## Confirmation pass

1. HIGH — [workspace.svelte.ts:2401](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2401), [workspace.svelte.ts:2410](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2410), [workspace.svelte.ts:2600](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2600), [workspace.svelte.ts:2606](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2606), [workspace.test.ts:3078](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.test.ts:3078), [workspace.test.ts:3128](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.test.ts:3128)  
   Concrete failure: the fix protects the await of `commands.duplicateMatch`, but not the second await of `commands.getDocument` inside `adoptTheDocumentOnDisk`. `followClone` is computed before that re-read begins and reduces the captured object/generation pair to a `MatchId | null`; after the await, the helper compares only the current selection's identity with that target. Start on the source, let the duplicate command answer, then while its document re-read is deferred select another snippet and return to the source. The pre-read `followClone` remains true, the post-read identity comparison succeeds, and the helper selects `moved`, reclaiming the selection for the clone even though two newer intents occurred. The same defect admits a failed `select()` during the re-read: it advances `selectGeneration` without necessarily replacing the selected object, but the helper no longer has the generation to check. The two new tests genuinely defer the command and correctly cover the histories they name across that first await; their `getDocument` remains the immediately answering scripted implementation, so neither drives the adoption await. This falsifies §6.1's unqualified claim that the clone is followed only while the captured object and generation still hold.  
   Smallest fix: carry the captured `SelectedMatch` reference and `selectGeneration` through the adoption re-read, and require both still to match immediately before following `moved`; keep passing `moved` independently as the attribution voucher. Add a deferred-`getDocument` leave-and-return case and a failed-selection-intent case.

2. LOW — [matchDuplication.test.ts:393](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/matchDuplication.test.ts:393), [matchDuplication.test.ts:394](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/matchDuplication.test.ts:394)  
   Concrete failure: the new test introduced for the `moved: null` arm still says that a committed null means “the file changed again between the write and the read.” Its fixture is only `saved(true, null)` and contains no read history, so that premise is neither true nor tested; the new Rust boundary test proves it false by producing the same answer after a failed re-read with no second writer. The production JSDocs, Rust method doc, and both localized `duplicatedNotIdentified` sentences now make the appropriately non-exhaustive claim, but the test suite still records the old false certainty. The shared `after_a_save` prose at [commands.rs:1339](/Users/ccarpio/Developer/espansoConfig/src-tauri/src/commands.rs:1339) also retains that claim; it is byte-for-byte pre-existing at `e079161`, so it is residue rather than a regression of this fix round.  
   Smallest fix: reword the test comment to say only that the clone could not be identified in the following read, with possible causes non-exhaustive. Repair the shared-tail prose in the owning operation's follow-up rather than using it as evidence for this duplicate.

### Verified without another finding

- The F1 capture uses the correct global `selectGeneration`, not the per-document `projectionGenerations`. Requiring the initiating selection to be the source changes only whether the wrapper follows the clone: a duplicate invoked while another pane/snippet holds selection still commits and runs ordinary R27 repair. When the adoption receives no target, a successful re-read installs the new projection and synchronously repairs or clears an old identity, so that path does not retain a `MatchId` naming nothing selected.
- The Rust failed-re-read test deletes the actual fixture after the workspace cached it and calls the shared post-save tail directly. A propagated deletion failure, a non-`Saved` result, or a non-null identity would fail the test. The wrapper's `moved: null` case re-reads revision `rev-b`, completes adoption, keeps the repaired source, and uses the external `kept` notice.
- F3's disk oracle is independent: the retained temporary directory names `match/base.yml`, `std::fs::read` checks that path against the expected bytes, and the separate `document_text` assertion still checks that the IPC cache serves those same expected bytes.
- F4 is complete on the changed surface. `RepairAttribution` says “changed the file”; member docs and bilingual notices retain the move/reorder versus duplicate/growth distinction. Searches found no sentence that describes a duplicate itself as reordering.
- The §6 residue about `moveMatch` is genuinely pre-existing: its current-selection comparison is unchanged by this step, while the common notice dispatch was extended without changing the move arms. `browser.matchMove.movedNotIdentified` and `browser.matchCreation.createdNotIdentified` are also unchanged from `e079161`; this step did not make either claim worse. Existing move tests were not edited by the F1 fix.
- The retabulated counts are correct: twelve frontend workspace commands, thirteen registered Rust commands including the menu command, six writers, seventeen command errors, 1,046 Rust tests, 1,300 frontend tests, 45 frontend test files, and 169 production modules.

### Checks run

- `git diff --check`
- `cargo test --workspace` — 1,046 passed, 0 failed
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo fmt --check`
- `npm test -- --run` — 1,300 passed, 0 failed, 45 files
- `npm run check` — 409 files, 0 errors, 0 warnings
- `npm run build` — 169 modules
- `cargo tree -p espansoconfig-core | rg tauri` — empty
- production bundle search for `svelte/internal/server` — empty

READINESS: NOT READY

## Round 4 — scoped pass

No findings.

### Verified without another finding

- The duplicate wrapper captures the exact `SelectedMatch` object and the global `selectGeneration` synchronously before `commands.duplicateMatch`, then passes the pair whole to the dedicated adoption ([workspace.svelte.ts:2372](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2372), [workspace.svelte.ts:2432](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2432)). `adoptAfterTheDuplicate` performs its only await first, installs the fresh projection, and then requires both `selected === intent.held` and `selectGeneration === intent.generation`; `positionInSameParse`, `selectMatch`, and `replaceSelection` are synchronous, so no await exists between re-validation and the write ([workspace.svelte.ts:2760](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2760), [workspace.svelte.ts:2765](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2765), [workspace.svelte.ts:2769](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2769), [workspace.svelte.ts:2780](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2780)). A separately minted, field-identical selection cannot satisfy the reference comparison. A failed re-read returns without a selection write and the caller drops the replaced document; every successful no-follow path synchronously runs `repairAfter`, which either installs a current identity or clears selection ([workspace.svelte.ts:2761](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2761), [workspace.svelte.ts:2785](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2785), [workspace.svelte.ts:2446](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2446), [workspace.svelte.ts:3042](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:3042)).
- Both new tests genuinely enter the adoption await before introducing the later intent: the intercepted `getDocument(2)` resolves `adoptionStarted` immediately before returning the deferred promise, and each test awaits that signal before acting ([workspace.test.ts:3184](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.test.ts:3184), [workspace.test.ts:3196](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.test.ts:3196), [workspace.test.ts:3217](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.test.ts:3217), [workspace.test.ts:3258](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.test.ts:3258), [workspace.test.ts:3266](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.test.ts:3266), [workspace.test.ts:3286](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.test.ts:3286)). The first replaces the held object twice; the second asserts the failed `select()` leaves that object unchanged, isolating the generation check ([workspace.test.ts:3220](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.test.ts:3220), [workspace.test.ts:3279](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.test.ts:3279), [workspace.test.ts:3289](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.test.ts:3289)). Against the round-2 shape described in the confirmation pass, both histories leave the current source identity eligible for the reduced-target comparison and therefore select the clone, so both tests would fail there.
- The extraction does not alter the shared move adoption: `moveMatch` still calls `adoptTheDocumentOnDisk`, while only the duplicate's committed adoption calls the new helper ([workspace.svelte.ts:2066](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2066), [workspace.svelte.ts:2432](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:2432)). The `moved: null` test comment now states only that the clone could not be identified and gives causes as non-exhaustive ([matchDuplication.test.ts:393](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/matchDuplication.test.ts:393)). Notes §6.6 accurately limits the repaired guarantee to re-validation after the adoption await and synchronous follow/repair, and §6.7's reported gates match this tree ([2c-3c-2-notes.md:345](/Users/ccarpio/Developer/espansoConfig/docs/decisions/2c-3c-2-notes.md:345), [2c-3c-2-notes.md:382](/Users/ccarpio/Developer/espansoConfig/docs/decisions/2c-3c-2-notes.md:382)).

### Checks run

- `git diff --check`
- `cargo test --workspace` — 1,046 passed, 0 failed
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo fmt --check`
- `npm test -- --run` — 1,302 passed, 0 failed, 45 files
- `npm run check` — 409 files, 0 errors, 0 warnings
- `npm run build` — 169 modules

READINESS: READY
