# Review brief — step 4-11 (Variable group, chips and choice controls)

- **Repo:** /Users/ccarpio/Developer/Utils/espansoConfig (macOS Tauri v2 app, Svelte 5 + TS frontend, Rust core). Rules: `CLAUDE.md`.
- **Phase and goal:** step 4-11 of `docs/decisions/4-split-notes.md` §2 (`### 4-11`, line ~263): the *Variables and fill-ins* group inside the match editor — ordered list with dependency state and "analysis incomplete", chips under content, add/remove/reorder controls, **Choice** insertion. Acceptance (mounted): controls not mounted until selected; every declaration stays reachable; a provisional name labelled "available among visible names" under an open scope; the compound insertion reaches one save; reorder cannot bypass the pending-draft rule or R25; conflict and recovery states draw. Also the items 4-9 owed (`docs/decisions/4-9-notes.md` §5/§8): sending `moveVariable` and handling its outcome, the `matchAuthoringSnapshot`/`analyzeMatchCandidate` wrappers in `BrowserCommands`, B1's mounted variable-list case, the quoted-`inject_vars` display decision.
- **Record of the step:** `docs/decisions/4-11-notes.md` (decisions §3, open items §5).
- **Changed files (uncommitted tree):** new `src/lib/browser/variableGroup.ts`, `src/lib/components/VariableGroup.svelte`, `src/lib/browser/variableGroup.test.ts`, `src/lib/components/MatchEditorVariables.test.ts`, `docs/decisions/4-11-notes.md`; modified `src/lib/browser/{matchEditor,workspace.svelte,variableEditor,fixtures}.ts`, `src/lib/components/{MatchEditor,DetailPane}.svelte`, `src/lib/i18n/{en,es}.json`, `src/lib/i18n/index.ts`, `scripts/lint/composition-guards.test.ts`, and several component test files. No Rust changed. (`PROGRESS.json` is a workflow record — ignore.)
- **Verification run (all exit 0):** `cargo test --workspace -- --test-threads=1` 1734 passed; clippy `-D warnings`; `cargo fmt --check`; `npm run check` 497 files 0/0; `npm test` 4240 passed; `npm run build` 220 modules; bundle oracle correct; core has no tauri dependency.
- **Risks to probe:** (1) a committed `moveVariable` write ever reported as an error, or a reorder reaching the wire while a draft is pending / combined with another edit (R25), or across a stale revision (D2v/R36/R37 — view, options and submission from one read); (2) a declaration unreachable in the list/chips (removed, duplicated names, unreadable names, open scope); (3) the Choice compound insertion producing more than one save or more than one undo step, or leaving ownership stale; (4) `\r` handling in any new input/textarea; (5) hardcoded user-facing strings or hand-built i18n keys; ES/EN parity; (6) the quoted-`inject_vars` display violating D2u or writing a value the user did not edit; (7) the section-key change in `MatchEditor.svelte` regressing other sections; (8) claims in the notes or comments that the code does not enforce (this project's worst defect class).
- **Review file:** `docs/reviews/4-11.md`
- **Time budget:** 15 minutes.
