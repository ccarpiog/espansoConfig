# Review brief — step 4-5 (variable lists and labelled choices)

- **Repo:** /Users/ccarpio/Developer/Utils/espansoConfig (macOS Tauri v2 app editing espanso YAML; every edit is a byte-span replacement and everything outside the intended span must stay byte-identical). Review the **uncommitted** working tree against `HEAD` (`d028708`).
- **Phase and goal:** `docs/decisions/4-split-notes.md` §2 `### 4-5`. Delivers item edits, insertion and removal in `depends_on`, `random.choices`, `script.args`, `choice` string lists and `{label, id}` records (including edits of existing records). Acceptance: first, middle and last additions and removals; empty lists; changed survivors; several insertions at one boundary; string and record shapes stay distinct; an existing flow list stays flow or is refused by type (Phase 3 ruling 5); a flow list holding a comment keeps its `CommentInFlowCollection` refusal (C2) and a test pins that; unknown entries inside a record survive. Risk **high**, core-first, no UI caller.
- **Worker's record:** `docs/decisions/4-5-notes.md` (what changed, deviations, open items). Check its claims against the code — a record claiming a guarantee the code does not give is this project's worst defect class (`CLAUDE.md` §5).
- **Changed files:** new `crates/espansoconfig-core/src/draft/variable_list.rs`, `crates/espansoconfig-core/tests/variable_lists.rs`, `docs/decisions/4-5-notes.md`; modified `crates/espansoconfig-core/src/draft/{audit,error,match_draft,mod,new_variable,plan}.rs`, `crates/espansoconfig-core/src/patch/edit.rs` (incl. new `InsertItem::several`, a block-style requirement for inserted list items), `crates/espansoconfig-core/src/patch/edit/corrupted_candidate_tests.rs`, existing expectations in `crates/espansoconfig-core/tests/{draft_plan,draft_sequence,variable_intents}.rs`, `src-tauri/src/{dictionary_contract,wire_contract}.rs`, `src/lib/i18n/{codes.ts,draftCodes.test.ts,en.json,es.json}`, `src/lib/ipc/types.ts`. `PROGRESS.json` is a workflow record, ignore it.
- **Verification run by the orchestrator (all exit 0):** `cargo test --workspace -- --test-threads=1` 1648 passed / 0 failed; clippy `-D warnings`; `cargo fmt --check`; `npm run check` 487 files 0/0; `npm test` 4086; `npm run build` 214 modules; bundle oracle correct; `cargo tree -p espansoconfig-core` holds no tauri. Failing-first evidence for the one defect fix: `/private/tmp/4-5/failing-first-flow-item.txt`.
- **Risks to probe:** (1) byte preservation around inserted/removed items in block and flow lists, especially first/last positions, comments and blank lines owned by neighbours; (2) whether the verifiers would accept a corrupted candidate for the new shapes (records with unknown keys, several records at one boundary, record label/id rewrites); (3) YAML 1.1 ambiguous plain scalars introduced by inserted or rewritten strings (`yes`, `on`, `1e3`, …); (4) the changed existing test expectations in `draft_plan.rs`/`draft_sequence.rs`/`variable_intents.rs` — were refusals weakened legitimately?; (5) shape mixing (a string into a records list and vice versa, records outside `choice.values`); (6) new `DraftError` codes carry no key/value text and have EN+ES strings with full wire/dictionary/accessor coverage; (7) removal of the last item never leaves an unnoticed null.
- **Review file:** `docs/reviews/4-5.md`.
- **Time budget:** 15 minutes.
