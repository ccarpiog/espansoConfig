# Review brief — Phase 2d-7-10 (the consolidation, records only)

- **Repo:** /Users/ccarpio/Developer/Utils/espansoConfig (uncommitted tree; base `c5fed6b`).
- **Phase goal:** consolidate 2d-7 as specified in `docs/decisions/2d-7-split-notes.md` §2 *2d-7-10*
  (bound by §3 entries 5, 32, 33, 35, 36, 37; §5.4, §5.5, §5.14; §7). Records only; nothing launched.
- **Changed files (the review scope):**
  - `docs/decisions/2d-7-10-notes.md` (new): the 2d-7 matrix, the ES sentence inventory, the 2d-8
    deletion manifest with `ls` evidence, the carried-forward list, the classing of 2d-7-8's three and
    2d-7-9's nine open items, and the gate re-measurement with the instrument's share from a pristine
    `git archive HEAD` copy.
  - `docs/decisions/2d-6-window-readings-consolidated.md`: the §5.4 pre-edit-copies row.
  - `CLAUDE.md` §6: `save_document` location `persist/write.rs` → `persist/save.rs`.
  - `PROGRESS.json`: in-flight status marker only (ignore).
- **Not in scope, must not be touched:** the uncommitted instrument (`src-tauri/src/main.rs`,
  `src/main.ts`, `src-tauri/src/probe.rs`, `src/probe.ts`) — deliberately uncommitted, hook diff must
  stay `5 insertions(+), 1 deletion(-)`.
- **Verification run by the orchestrator:** `cargo test --workspace -- --test-threads=1` → 26
  `test result: ok` lines, no failures; `npm test` → 3547 passed; `npm run check` → 462 files, 0 errors
  0 warnings. The worker reports clippy/fmt/build pass, pristine 1323/461/3546/200 vs tree
  1330/462/3547/201, hash check `summary ok=18 diff=0`.
- **Risks to check (adversarially):**
  1. Completeness: is every item-7 clause, every consolidated §5.1/§5.2 row and every G row present
     **exactly once**? Cross-check against the source records (2d-7-5 … 2d-7-9 notes, the consolidated
     record, the split notes).
  2. Honesty (the project's worst defect class): does any row claim *read* where the source records
     say unread / read in part / constructed? Is each read-in-part row's unread half carried as unread?
     Is every constructed and unread row in the carried-forward list?
  3. The manifest matches entry 32 as corrected by §5.14 (eight pre-edit files), with `ls` evidence
     and "checked and reported, never deleted" entries kept as such.
  4. The ES inventory texts match `src/lib/i18n/es.json` for the keys cited.
  5. The instrument-share arithmetic and its explanation are consistent.
  6. No real espanso config content quoted anywhere.
- **Review file:** `docs/reviews/2d-7-10.md`.
- **Time budget:** 15 minutes.
