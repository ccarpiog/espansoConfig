Reviewer: codex adversarial review

Codex adversarial review of the working tree at /Users/ccarpio/Developer/Utils/espansoConfig.
Codex verdict: needs-attention. 4 finding(s): 1 blocker(s), 3 should-fix.

do-not-ship: reproduced stale-projection rollback and an external-evidence pairing bypass. Two claimed type/provenance guarantees are also false. No files modified; green gates were not rerun.

- [BLOCKER] high · confidence 1 — src/lib/browser/workspace.svelte.ts:3575 — 1. Re-registering an observation revives a stale adoption
    rememberExternalConflict unconditionally overwrites the source's recorded projection generation through rememberTheConflict. Reproduced with the actual BrowserState: register an ob…
    Fix: Preserve the original registration generation for an already-registered source; repeated registration must not renew its authority. Add a re…
- [SHOULD-FIX] medium · confidence 1 — src/lib/browser/reapply.ts:425 — 2. Evidence can change between the pairing checks and return
    The base comparison is followed by caller-controlled property reads, and the accepted result returns the original mutable table. Reproduced using describeExternalConflict with a ta…
    Fix: Capture an owned, immutable evidence snapshot and the comparison operands before validating them, then return that validated snapshot. Inclu…
- [SHOULD-FIX] medium · confidence 1 — docs/decisions/2d-5-5a-notes.md:59 — 3. The record claims structural exclusion that TypeScript does not enforce
    The assertion that ExternalConflictModel cannot be given expected is false. An in-memory compiler probe using this repository's TypeScript and compiler options accepted `const exte…
    Fix: Correct the record to distinguish inaccessible fields from prohibited properties. If strict exclusion is required, encode it in the model ty…
- [SHOULD-FIX] medium · confidence 1 — src/lib/browser/saveOutcome.ts:1087 — 4. Hand-built observations are not guaranteed to fail adoption
    This new comment says a hand-built observation is absent from the origin map and therefore installs nothing. The exported rememberExternalConflict accepts precisely such an observa…
    Fix: State accurately that only unregistered observations fail adoption and that registration trusts its caller. If provenance rejection is inten…

Scope: the working tree only — anything already committed for this phase was outside it.

VERDICT: ship-with-fixes
BLOCKERS: src/lib/browser/workspace.svelte.ts:3575 — 1. Re-registering an observation revives a stale adoption — Preserve the original registration generation for an already-registered source; repeated registration must not renew its authority. Add a re…
SHOULD-FIX: src/lib/browser/reapply.ts:425 — 2. Evidence can change between the pairing checks and return — Capture an owned, immutable evidence snapshot and the comparison operands before validating them, then return that validated snapshot. Inclu…
SHOULD-FIX: docs/decisions/2d-5-5a-notes.md:59 — 3. The record claims structural exclusion that TypeScript does not enforce — Correct the record to distinguish inaccessible fields from prohibited properties. If strict exclusion is required, encode it in the model ty…
SHOULD-FIX: src/lib/browser/saveOutcome.ts:1087 — 4. Hand-built observations are not guaranteed to fail adoption — State accurately that only unregistered observations fail adoption and that registration trusts its caller. If provenance rejection is inten…
NOT-VERIFIED: Fix blockers 1–4 and add the targeted regression cases before shipping this phase.
NOT-VERIFIED: The new memo-identity, model-field/message/draft, evidence-refusal, and external-adoption tests are load-bearing for their asserted behavior. The choice-list te…
NOT-VERIFIED: The save-only SaveOutcomeModel/beginReapply restrictions and i18n accessor placement are consistent with this phase's scope. Missing production wiring and rulin…
