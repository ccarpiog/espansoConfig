- Verdict (Q1): **No** to a generic second YAML parser. Syntax-only reparsing is largely theatre because untouched bytes are identical and emitted scalars are conservatively quoted. The real residual defect is specific: a pre-existing plain scalar such as `on`, `012`, or `12:30` is projected as a string by the app but resolved by Espanso as a boolean or integer. Only Espanso’s actual semantic deserializer—or a faithfully reproduced resolver—can catch that. A second parser could also expose omissions in the emitter’s safety predicate, but a targeted resolver test catches those more directly.

- Verdict (Q2): **Adopt no independent crate merely because it says “YAML 1.1.”** If behavioral parity is later required, try the exact `serde_yaml` version used by Espanso—plausibly the `0.8.x` era for older Espanso releases—as a dev-dependency confined to one integration-test module, and compare fully materialized values rather than parse success. Verify the version locally: `serde_yaml` `0.9.x` is deprecated/unmaintained and uses `unsafe-libyaml`; older `0.8.x` used `yaml-rust`. `unsafe-libyaml` is a Rust translation of libyaml, and `libyaml-safer` is a wrapper, but libyaml’s event parser does not itself provide a complete application-level YAML 1.1 implicit-type resolver. `yaml-rust` `0.4` is old/unmaintained and its resolver is not reliably full YAML 1.1; `yaml-rust2` and saphyr are maintained successors aimed at YAML 1.2. My maintenance details may be stale, and Espanso’s exact dependency/version is uncertain; its own locked deserialization stack is the only crate choice worth treating as authoritative.

- Verdict (Q3): **Build the in-house tag-resolution oracle.** It is stronger than an unrelated parser for R16 because it directly tests the disputed boolean, null, integer/octal, float, sexagesimal, and timestamp resolution rules and can exhaustively check every plain scalar. Do not require the existing corpus to contain zero ambiguous scalars—that would reject legitimate 1.1-oriented Espanso files. Report and mark pre-existing ambiguities in the projection, while hard-failing if an edit introduces a new ambiguous plain scalar or changes its classification. Combine it only with Espanso’s exact deserializer when that dependency is identified; do not combine it with a nominal “1.1 parser” for reassurance.

- One honest sentence for R16 to go in PROGRESS.md’s risk table: **R16 stays open: byte preservation and conservative emission prevent edits from changing untouched bytes or introducing known YAML 1.1-ambiguous plain scalars, but the UI projection of pre-existing plain scalars is not yet proven to match Espanso’s resolver.**

- The concrete residual risk that survives whatever is recommended, phrased for a risk table row: **A pre-existing or explicitly tagged scalar may be displayed or used by the typed projection with a different type/value than Espanso assigns, and an incomplete hand-maintained resolver table or an Espanso-specific schema change could leave that disagreement undetected.**

Codex session ID: 019fb438-e97c-7421-9ae9-d9afb1c1636f
Resume in Codex: codex resume 019fb438-e97c-7421-9ae9-d9afb1c1636f
