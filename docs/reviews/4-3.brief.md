# Review brief — step 4-3 (nested presence and author-key foundation)

- **Repo:** /Users/ccarpio/Developer/Utils/espansoConfig (review the **uncommitted** working tree against `HEAD` = `8d2d840`).
- **Phase and goal:** step 4-3 of `docs/decisions/4-split-notes.md` §2 (`### 4-3`, lines ~142-159; key rules in §3 ruling 7; narrowings in §4). Deliver four-state presence and location metadata (absent / empty / supported / unsupported shape) for `vars`, `params`, `depends_on`, shorthand `form_fields`, verbose `params.fields` and editable nested lists; lift D1 for bounded insertion and removal of an **author-named** scalar or scalar-list `params` entry under ruling 7. Core-first; no production UI caller.
- **Acceptance (from the split notes):** the four presence states differ on synthetic fixtures; Unicode and punctuation keys round-trip; a duplicate key refuses, including an equivalently quoted spelling and a pending insertion; `<<`, an empty key, CR/LF and a one-level-too-deep path refuse; every refusal carries positions and codes, never the key text; the audit's closed surface (`draft/audit.rs`) is widened by named shapes only, and a test one segment deeper than the new surface is refused.
- **Changed files:** `crates/espansoconfig-core/src/draft/{author_key (new),audit,error,match_draft,mod,new_match,plan}.rs`, `crates/espansoconfig-core/src/model/{match_view,mod,project,value,variable}.rs`, `crates/espansoconfig-core/src/validate/mod.rs`, `crates/espansoconfig-core/tests/{draft_plan,draft_params (new),nested_presence (new)}.rs`, `src-tauri/src/{wire_contract,dictionary_contract}.rs`, `src/lib/ipc/types.ts`, `src/lib/i18n/{en.json,es.json,draftCodes.test.ts}`, `src/lib/browser/{fixtures.ts,workspace.svelte.ts}`, notes `docs/decisions/4-3-notes.md`. `PROGRESS.json` is a workflow record.
- **Verification run by the orchestrator (all exit 0):** `cargo test --workspace -- --test-threads=1` 1593 passed; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo fmt --check`; `npm run check` 487 files 0/0; `npm test` 4086 passed; `npm run build` 214 modules; bundle oracle correct; `cargo tree -p espansoconfig-core` holds no tauri. Rung `1593 / 487 / 4086 / 214` (was `1567 / 487 / 4086 / 214`).
- **Worker-declared deviations to judge:** (1) an extra refusal `NewKeyIsATypedSetting` for new keys `offset`, `trim`, `debug`, `multiline`, `trim_string_values` (to avoid writing `trim: 'true'`); (2) `ParamsWouldBeEmpty` now also refuses removing every `params` entry, which was previously possible — one existing test expectation changed in `tests/draft_plan.rs`; (3) tests use inline synthetic YAML, no new corpus files; (4) nested insertions rely on the existing insertion-group verifier with no mutation test showing it catches a wrong nested rendering.
- **Risks to probe:** byte preservation outside the edited span for nested insertion/removal (indentation, comments, flow vs block `params`, CRLF, BOM, no trailing newline); duplicate-key detection across quoting/escapes; key emission (a key needing quotes must be quoted correctly, never a YAML 1.1-ambiguous plain key); the audit widening being exactly as wide as named; refusal payloads leaking key text; presence projection correctness for unsupported shapes (a scalar where a mapping is expected, flow collections, null); the regression from the changed existing expectation; any record/comment claiming a guarantee the code does not give (this project's worst defect class); CLAUDE.md rules (i18n EN+ES via accessors, JSDoc, closing-bracket comments, core never depending on tauri, real corpus never quoted).
- **Review file:** `docs/reviews/4-3.md`.
- **Time budget:** 20 minutes.
