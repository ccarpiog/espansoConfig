Reviewer: codex adversarial review

Codex adversarial review of the working tree at /Users/ccarpio/Developer/Utils/espansoConfig.
Codex verdict: needs-attention. 3 finding(s): 1 blocker(s), 2 should-fix.

Do not ship yet: existing boolean settings can become strings, and reapply misses concurrent comment edits and a compound-insertion lifecycle case.

- [BLOCKER] high · confidence 0.99 — src/lib/browser/variableEditor.ts:555 — Existing inject_vars edits use the logical-string writer
    Editing an existing inject_vars from false to true produces VariableDraft.inject_vars.Set("true"). Rust's plan_variable_scalar routes this through plan_scalar and ScalarEdit::new,…
    Fix: Give existing inject_vars edits a validated plain-source Rust planning path. Add an end-to-end draft/emission regression asserting false-to-…
- [SHOULD-FIX] medium · confidence 0.98 — src/lib/browser/variableEditor.ts:1375 — The container fingerprint excludes comments that removals delete
    Draft removing the first variable, then externally edit its owned leading comment. ContainerBaseline starts at the first item's value bytes, excluding that comment, so this compari…
    Fix: Include all owned bytes a structural removal can delete in its correspondence baseline, or check a separate removal fingerprint. Test concur…
- [SHOULD-FIX] medium · confidence 1 — src/lib/browser/matchEditor.ts:5448 — Content-switch cancellation breaks compound Insert tracking
    Reproduced with the model: switch replace to markdown, insert a variable into markdown, then cancel the switch. The reference moves back to replace, but added.insertedInto remains…
    Fix: Update compound insertion ownership whenever content switches move text, including cancellation and retargeting. Add regressions for both ha…

Scope: the working tree only — anything already committed for this phase was outside it.

VERDICT: ship-with-fixes
BLOCKERS: src/lib/browser/variableEditor.ts:555 — Existing inject_vars edits use the logical-string writer — Give existing inject_vars edits a validated plain-source Rust planning path. Add an end-to-end draft/emission regression asserting false-to-…
SHOULD-FIX: src/lib/browser/variableEditor.ts:1375 — The container fingerprint excludes comments that removals delete — Include all owned bytes a structural removal can delete in its correspondence baseline, or check a separate removal fingerprint. Test concur…
SHOULD-FIX: src/lib/browser/matchEditor.ts:5448 — Content-switch cancellation breaks compound Insert tracking — Update compound insertion ownership whenever content switches move text, including cancellation and retargeting. Add regressions for both ha…
NOT-VERIFIED: Fix the emission and reapply gaps, then run focused regressions and the existing verification suite.
