Reviewer: codex adversarial review

Codex adversarial review of the working tree at /Users/ccarpio/Developer/Utils/espansoConfig.
Codex verdict: needs-attention. 3 finding(s): 2 blocker(s), 1 should-fix.

ship-with-fixes — 2 source blockers and 1 record-only SHOULD-FIX. The lifecycle fence still permits old observations to contaminate a replacement workspace through injected getters. Reviewed committed 4f7c500; no files modified and no tests, builds, package, or Cargo commands run.

- [BLOCKER] high · confidence 0.99 — src/lib/browser/observationTransitions.ts:913 — Capture lifecycle ownership before caller-controlled batch reads
    Source finding: the new fence can compare the replacement lifecycle with itself. In reconciliationCoordinator.ts:917–919, accept() reads batch.newest_sequence before constructing s…
    Fix: Capture a trusted lifecycle token before reading the injected batch, and revalidate it after materialization before changing the watermark o…
- [BLOCKER] high · confidence 0.99 — src/lib/browser/observationTransitions.ts:878 — Fence routing before declaring other arms safe in isolation
    Source finding: the newly added 'safe in isolation' claim overlooks routeObservation(), which runs before every arm. A single Removed observation can have a sequence getter that ca…
    Fix: Revalidate the captured lifecycle immediately after routeObservation() and before dispatching any arm. Retain addition's later check because…
- [SHOULD-FIX] medium · confidence 1 — docs/decisions/2d-5-4-notes.md:523 — Correct the surviving identity-reallocation rationale in the record
    Record-only finding, confirmed independently of D-notes §9.3's nomination. This passage still justifies clearing statuses by claiming that open() reallocates their identities; the…
    Fix: Annotate the surviving assertions with the path-stable identity contract and each site's actual lifecycle rationale. Distinguish historical…

Scope: the working tree only — anything already committed for this phase was outside it.

VERDICT: ship-with-fixes
BLOCKERS: src/lib/browser/observationTransitions.ts:913 — Capture lifecycle ownership before caller-controlled batch reads — Capture a trusted lifecycle token before reading the injected batch, and revalidate it after materialization before changing the watermark o…
BLOCKERS: src/lib/browser/observationTransitions.ts:878 — Fence routing before declaring other arms safe in isolation — Revalidate the captured lifecycle immediately after routeObservation() and before dispatching any arm. Retain addition's later check because…
SHOULD-FIX: docs/decisions/2d-5-4-notes.md:523 — Correct the surviving identity-reallocation rationale in the record — Annotate the surviving assertions with the path-stable identity contract and each site's actual lifecycle rationale. Distinguish historical…
NOT-VERIFIED: Fix both lifecycle gaps and add focused regression cases during the fix round.
NOT-VERIFIED: Correct the decision-record claims without treating every historical search hit as a separate defect.
NOT-VERIFIED: A source-changing fix commissions a scoped review under CLAUDE.md §7.1; record-only corrections do not.
