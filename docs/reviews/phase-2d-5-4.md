Reviewer: codex adversarial review

Codex adversarial review of the working tree at /Users/ccarpio/Developer/Utils/espansoConfig.
Codex verdict: needs-attention. 5 finding(s): 2 blocker(s), 3 should-fix.

do-not-ship — 5 blockers: 2 high, 3 medium. Read-only review; no files changed and no tests or builds run.

- [BLOCKER] high · confidence 0.99 — src/lib/browser/workspace.svelte.ts:2596 — Caller-controlled reads still separate the final guard from installation
    After the final stillCurrent() check, this path reads fresh.value; installView then reads next.id. A valid injected CommandResult can have a value getter that registers a same-docu…
    Fix: Materialize the response and installation/repair operands before the final checks, then commit without caller-controlled reads. Add a worksp…
- [BLOCKER] high · confidence 0.99 — src/lib/browser/observationTransitions.ts:858 — Lost-history blocking does not invalidate pending rereads
    Keep a surface open over document B and start a deferred clean reread of A. Before that reread resolves, accept a batch with increased discarded. The coordinator enters blockedByLo…
    Fix: Expose coordinator application validity to the guard and invalidate outstanding observation work on lost-history blocking and disposal. Chec…
- [SHOULD-FIX] medium · confidence 0.99 — src/lib/browser/workspace.svelte.ts:2429 — Pending Added identities escape into document commands through the viewer
    Adding the unaddressable summary to documents makes it eligible for rawTarget(), which returns any matching summary (rawDocument.ts:62-73). Select that new row and enable the raw v…
    Fix: Retain explicit pending-membership/addressability state and prevent pending identities from entering document-command targets until a succes…
- [SHOULD-FIX] medium · confidence 0.99 — src/lib/browser/workspace.svelte.ts:2075 — Discarding the reread result hides a failed reconciliation
    A Changed/Projected observation advances the accepted sequence and batch watermark before this fire-and-forget call. If reloadDocument returns a failure, rereadUnderGuard reports i…
    Fix: Handle the reread outcome, or mark the document stale when the read starts and clear it only after successful guarded installation. Fence fa…
- [SHOULD-FIX] medium · confidence 1 — src/lib/browser/workspace.test.ts:8245 — The required workspace routing evidence is incomplete and partly vacuous
    readsAtOpen and opensAtStart are captured after settleDrains() has processed the observation batch. An erroneous getDocument/openWorkspace call during that batch is already include…
    Fix: Capture command baselines before delivering observations through a controlled wake. Assert all relevant document commands, complete the work…

Scope: the working tree only — anything already committed for this phase was outside it.

VERDICT: ship-with-fixes
BLOCKERS: src/lib/browser/workspace.svelte.ts:2596 — Caller-controlled reads still separate the final guard from installation — Materialize the response and installation/repair operands before the final checks, then commit without caller-controlled reads. Add a worksp…
BLOCKERS: src/lib/browser/observationTransitions.ts:858 — Lost-history blocking does not invalidate pending rereads — Expose coordinator application validity to the guard and invalidate outstanding observation work on lost-history blocking and disposal. Chec…
SHOULD-FIX: src/lib/browser/workspace.svelte.ts:2429 — Pending Added identities escape into document commands through the viewer — Retain explicit pending-membership/addressability state and prevent pending identities from entering document-command targets until a succes…
SHOULD-FIX: src/lib/browser/workspace.svelte.ts:2075 — Discarding the reread result hides a failed reconciliation — Handle the reread outcome, or mark the document stale when the read starts and clear it only after successful guarded installation. Fence fa…
SHOULD-FIX: src/lib/browser/workspace.test.ts:8245 — The required workspace routing evidence is incomplete and partly vacuous — Capture command baselines before delivering observations through a controlled wake. Assert all relevant document commands, complete the work…
NOT-VERIFIED: Fix the four source defects and supply the missing regression and acceptance evidence.
NOT-VERIFIED: Correct the decision record's atomicity, command-reachability and coverage claims; keep the phase BLOCKED until the findings are resolved.
