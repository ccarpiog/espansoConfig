1. MEDIUM — [MatchDuplicator.svelte:443](/Users/ccarpio/Developer/espansoConfig/src/lib/components/MatchDuplicator.svelte:443), [matchDuplication.ts:1000](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/matchDuplication.ts:1000), [matchDuplication.ts:1088](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/matchDuplication.ts:1088)  
   **Demonstrated.** The component is not a rule-free walk over its model: it decides that the frozen `notDuplicable` reason loses when the live refusal is `outOfDate`. Open the panel over a read-only projection and then replace that projection with a fresh revision. `matchDuplicationView` returns both `notDuplicable: 'readOnly'` and `cannotDuplicate: 'outOfDate'`; line 443 is the only code that prevents the stale, definite read-only claim from being rendered beside the weaker live claim. The mounted test at `MatchDuplicator.test.ts:512` consequently tests decision logic in the `.svelte` file rather than a complete model answer. This violates the project's binding architecture rule and leaves any second renderer—or a harmless-looking markup refactor—able to reinstate the contradictory claim even while walking the model faithfully.  
   Smallest fix: make `matchDuplicationView` suppress the frozen reason when `cannotDuplicate === 'outOfDate'` (either by returning `notDuplicable: null` or by adding an explicitly presentation-ready field), add the stale/read-only assertion to `matchDuplication.test.ts`, and render that field directly without the second condition in the component.

2. LOW — [matchDuplication.ts:201](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/matchDuplication.ts:201), [matchDuplication.ts:233](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/matchDuplication.ts:233), [DetailPane.svelte:407](/Users/ccarpio/Developer/espansoConfig/src/lib/components/DetailPane.svelte:407), [en.json:349](/Users/ccarpio/Developer/espansoConfig/src/lib/i18n/en.json:349), [es.json:349](/Users/ccarpio/Developer/espansoConfig/src/lib/i18n/es.json:349)  
   **Demonstrated.** `documentHasUnsavedDraft` does not measure unsaved edits; it returns `true` for every open match editor identity, and `DetailPane` explicitly says it cannot observe whether that editor is dirty. Pass the identity of a pristine editor whose buffers still equal its baseline and the producer returns `true`, after which the panel says that the snippet has edits which have not been saved (`Un fragmento ... tiene cambios sin guardar` in Spanish). The conservative refusal is safe, but the sentence is false of the predicate, and Q6/Q8 asked specifically about a dirty draft. This is currently latent in the integrated pane because `busy` prevents an editor and duplicator from being open together; the direct component fixture at `MatchDuplicator.test.ts:476` can render the arm only by supplying `true` itself.  
   Smallest fix: either lift the editor's dirty fact to `DetailPane` and pass only dirty draft identities to `documentHasUnsavedDraft`, or rename the model arm and both localized sentences to make the deliberate policy truthful (for example, that a snippet editor is open and must be closed first). Keep the raw editor out of this predicate only while `busy` continues to make the raw and duplicate surfaces mutually exclusive.

## Verified without another finding

- `MatchDuplicator` derives its live view and the identity supplied to `beginDuplicate` from the `current` projection value, whose `$derived.by` body calls `projections()` once. The submission uses the session's frozen base revision. No second live-projection lookup is made in `runDuplicate`, so the one-synchronous-read hole is closed apart from finding 1's model/presentation split.
- The component calls `BrowserState.duplicateMatch`, not the raw IPC wrapper. It performs no selection write after that await, so it does not reopen the selection-follow race closed in step 2. The changed files introduce no write to `selected`, `selectGeneration`, or `projectionGenerations`.
- The acknowledgement is produced by `acknowledgeDuplicationFindings` and extracted from the `DraftSubmission`; the component never reconstructs a finding or a revision. The mounted retry test compares the complete accepted finding, including `DuplicateKeepsTriggerDefinition.revision`.
- The six write surfaces are mutually exclusive in both directions. `busy` includes the duplicate session, so the creation opener is withdrawn, and the single `if`/`else if` surface chain prevents every other write surface and its opener from rendering while duplicate is open. Conversely, an existing raw editor, match editor, deleter, mover, or creator keeps the duplicate opener out of the rendered branch. The file-text toggle can remain visible, but it cannot open another writer while the duplicate branch owns the pane.
- Excluding the whole-document raw editor from `documentHasUnsavedDraft` is safe in the current product because that editor and duplicate are mutually exclusive. It would become a real stranding hole if that exclusivity were relaxed; finding 2 is instead about the predicate's open-versus-dirty mismatch and the false sentence it can produce.
- The `path: matchListPath(0)` additions in `DetailPane.test.ts` only make the fixtures genuine sequence items. Existing editor, selection, read-only and deletion assertions retain their original inputs and oracles; no existing assertion was removed or weakened.
- All 31 `browser.matchDuplication.*` keys exist in both dictionaries. Static labels use the typed `t` accessor; refusal, submission-refusal and recovery codes go through exhaustive typed key builders; findings, failures, notes, verdicts and conflict choices use their typed accessors. All 31 keys are reachable from the component/model/DetailPane surface, and no component constructs an i18n key. No hardcoded user-facing string was found.
- A committed result stays an outcome even when adoption fails, and `landed === null` adds only the content-limited `duplicatedNotIdentified` sentence. A non-null `landed` suppresses that warning; no reversible or undo control is offered after the structural insertion.
- Duplicate introduces no text drafting control, so textarea/input line-ending normalization is not implicated.
- The manual bilingual window reading should specifically check the sticky action row at the target height, the long Spanish `unsavedDraftInDocument`, `mayHaveWritten`, `outOfDate`, and `duplicatedNotIdentified` paragraphs, and the committed-with-failed-adoption layout where `fileWritten`, `windowOutOfStep`, and the duplicate outcome appear together without visual ambiguity.

## Checks considered

- The supplied evidence was accepted without rerunning it: `npm test` (1,322 passed / 46 files), `npm run check` (0 errors, 0 warnings), `npm run build` (171 modules), `cargo test --workspace` (1,046 passed), `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo fmt --check`.
- Review inspection used the uncommitted diff for the four modified files and full reads of both new files, plus the requested rule sheet, design/decision records, mover precedent, workspace wrapper and i18n surface.

READINESS: NOT READY

## Confirmation review round 2 — the two fixes only

Finding 1's behavior is closed, not relocated. `matchDuplicationView` now computes the presentation-ready `notDuplicableToShow` from the same `cannotDuplicate` answer that drives `canDuplicate`; `MatchDuplicator.svelte` performs only the null check. No production transition can put a refused eligibility beside `mayHaveWritten`, `alreadyDuplicated`, `saveInFlight`, or `conflict`: the refused session cannot pass `beginDuplicate`, so none of the component's answer transitions can follow. The two reachable `outOfDate` routes correctly suppress a frozen claim that can no longer be vouched for. The raw verdict remains intact on `MatchDuplicationSession.eligibility`, and the two new model cases have non-vacuous live halves and would fail against the old view, which returned the frozen reason on both stale routes. Removing the old view member weakened no caller or test found in the repository.

Finding 2's user-visible claim is corrected in both languages. English now states the open-editor predicate exactly. The Spanish is natural, makes the same open-versus-dirty distinction, and uses the conditional `lo que no se haya guardado` rather than asserting that edits exist. The dictionaries have all 699 keys in exact parity and no placeholder mismatch. The touched model, prop, parameter, and coordinator comments consistently describe an open editor rather than a dirty one. The remaining qualification is finding 2 below.

1. LOW — [matchDuplication.ts:9](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/matchDuplication.ts:9), [matchDuplication.test.ts:783](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/matchDuplication.test.ts:783), [MatchDuplicator.test.ts:1](/Users/ccarpio/Developer/espansoConfig/src/lib/components/MatchDuplicator.test.ts:1)  
   **Demonstrated.** The fix introduced a false testability record. The model header says a decision in markup is something “nothing can check,” and the new model test repeats that no automated test in the repository can drive `MatchDuplicator.svelte`. But `MatchDuplicator.test.ts` explicitly opts into jsdom, mounts that component, and its live/stale pair at lines 499–533 drove the old component condition in round 1. The architectural problem was that the rule belonged to the renderer and could be omitted by a second renderer, not that this renderer was untestable. A later maintainer following the new comments could discard or discount the mounted regression coverage on a premise the repository itself disproves.  
   Minimal fix: say that model tests cannot drive markup and that a second renderer could omit the rule; preserve the accurate distinction that the mounted suite checks this renderer while the model now owns the reusable decision.

2. LOW — [2c-3c-2-notes.md:101](/Users/ccarpio/Developer/espansoConfig/docs/decisions/2c-3c-2-notes.md:101), [phase-2c-3c-design.md:156](/Users/ccarpio/Developer/espansoConfig/docs/reviews/phase-2c-3c-design.md:156), [phase-2c-3c-design.md:247](/Users/ccarpio/Developer/espansoConfig/docs/reviews/phase-2c-3c-design.md:247)  
   **Demonstrated; round-1 finding 2 is not fully closed.** The shipped sentences and code comments are now truthful, but the governing records still say that the coordinator supplies the fact that a dirty draft exists and list “Document-wide dirty-draft coordination” as a completion criterion. In the concrete pristine-editor scenario from round 1, `documentHasUnsavedDraft` returns `true` although there is no dirty draft, so those records claim a guarantee the implementation deliberately does not give. This leaves the same open-versus-dirty defect as a false decision record even though it no longer reaches the duplication UI sentence.  
   Minimal fix: amend both records to say that the intended risk is a dirty draft but the implemented, deliberately conservative predicate measures any open match editor because the coordinator cannot observe `isDirty`.

No finding changes the owed manual reading beyond its existing scope: it should still read the long English and Spanish `unsavedDraftInDocument` paragraphs as conditional open-editor warnings, alongside the sticky action row and the previously named failure/outcome combinations.

The supplied round evidence was accepted without rerunning it: 1,324 frontend tests, clean checks/build, 1,046 Rust tests, clean Clippy/format, and the hardcoded-string scan.

READINESS: NOT READY
