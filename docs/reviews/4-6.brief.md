# Review brief — step 4-6, form-definition structural core

- **Repo:** /Users/ccarpio/Developer/Utils/espansoConfig (uncommitted working tree against HEAD `6f92dea`)
- **Phase and goal:** step 4-6 of Phase 4 (`docs/decisions/4-split-notes.md` §2 `### 4-6`). Insertion and
  removal of form field definitions and their scalar/list options in shorthand `form_fields` and verbose
  `params.fields`, and complete new verbose form descriptions — in the core crate, no production UI caller.
- **Acceptance (split notes):** one intention writes shorthand or verbose shape, never converting; a layout
  edit plus a field addition is one batch; deleting a definition touches only its owned span; a scalar-list
  `values` and a multiline-string `values` (`tests/corpus/synthetic/form-layout-and-choice.yml:43-54`) keep
  their representation; unsupported subtrees refuse structured rewriting.
- **Record:** `docs/decisions/4-6-notes.md` (what changed, what is not guaranteed, open items).
- **Changed files:** new `crates/espansoconfig-core/src/draft/{form_definition,form_plan}.rs`,
  `crates/espansoconfig-core/tests/form_definitions.rs`; modified `src/patch/edit.rs` (ItemValue mappings
  may now nest; mapping-valued entries in insertion groups; per-level recursive verification),
  `src/patch/edit/corrupted_candidate_tests.rs`, `src/draft/{mod,match_draft,new_variable,plan,audit,error,author_key,new_match}.rs`,
  core tests `draft_params.rs`, `draft_plan.rs`, `draft_sequence.rs`, `variable_intents.rs`;
  `src-tauri/src/{dictionary_contract,wire_contract,retained_state_contract}.rs`; `src/lib/ipc/types.ts`,
  `src/lib/i18n/{en,es}.json`, `src/lib/i18n/{codes.ts,draftCodes.test.ts}`, `src/lib/browser/matchEditor.ts`
  and three TS test fixtures. 16 new `DraftError` codes (82 → 98); eight existing EN/ES `newKey*` sentences reworded.
- **Verification run (orchestrator, all exit 0):** `cargo test --workspace -- --test-threads=1` 1668 passed;
  clippy `-D warnings`; `cargo fmt --check`; `npm run check` 487 files 0/0; `npm test` 4086; `npm run build`
  214 modules; bundle oracle correct; `cargo tree -p espansoconfig-core` holds no tauri.
- **Risks to probe hardest:**
  1. The engine change in `patch/edit.rs`: nesting `ItemValue` mappings is now unbounded by the type — can
     the audit or verifier be bypassed so a candidate that changes bytes outside the intended span, or
     inserts an ambiguous YAML 1.1 plain scalar (other than a requested plain-source node such as
     `multiline`/`trim_string_values`), is accepted?
  2. Shorthand vs verbose: can any intent convert one shape into the other, or write into the wrong one?
  3. Definition removal: is the owned span exact (comments, blank lines, last item, flow collections,
     no-final-newline files)? Removing the last definition must not leave an invalid/null form.
  4. `values` representation: a list stays a list, a multiline string stays a block scalar.
  5. Unsupported subtrees (per 4-3's four-state presence) refused by name, not rewritten.
  6. Refusal codes carry no key/value text; EN+ES present; records claim nothing the code does not force.
  7. Failing-first was done by mutation, not by the unchanged tree (the worker could not use git) — judge
     whether the tests actually discriminate.
- **Review file:** `docs/reviews/4-6.md`
- **Time budget:** 15 minutes.
