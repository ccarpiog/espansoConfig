# Review brief — step 4-12, visual form builder

- **Repo:** /Users/ccarpio/Developer/Utils/espansoConfig (macOS Tauri v2 + Svelte 5 + Rust; edits espanso YAML byte-preservingly). Rules: `CLAUDE.md` (read §2, §5, §6 in full).
- **Phase:** 4-12 of Phase 4 (variables and forms). Spec: `docs/decisions/4-split-notes.md` §2 `### 4-12` — the synchronized layout display with derived rows, first-class Choice/List field controls, explicit field operations and Form insertion, for both storage shapes (shorthand `form` + `form_fields`; verbose `type: form` variable with `params.layout`/`params.fields`). Acceptance (mounted): both shapes; layout-only edits; explicit compound additions; definition-only rows; unknown source visible through `SourceText`; `multiline`, defaults and trimming as textual controls; conflict compare; the recovery refusal. It also takes 4-10's owed items (`docs/decisions/4-10-notes.md` §6): `values` items, option removal, remove-all. Window half owed to 4-13 (not in scope).
- **Worker's record:** `docs/decisions/4-12-notes.md` (§3 decisions/deviations, §5 open items).
- **Changed files (uncommitted):** new `src/lib/browser/formBuilder.ts`, `src/lib/components/FormBuilder.svelte`, `src/lib/browser/formBuilder.test.ts`, `src/lib/components/MatchEditorForms.test.ts`, `docs/decisions/4-12-notes.md`; modified `src/lib/browser/{formEditor,matchEditor,variableInsertion}.ts`, `src/lib/browser/formEditor.test.ts`, `src/lib/components/MatchEditor.svelte`, `src/lib/i18n/{index.ts,en.json,es.json}`, `scripts/lint/composition-guards.test.ts`, `crates/espansoconfig-core/tests/form_definitions.rs`. `PROGRESS.json` is a workflow record, not in scope.
- **Verification run by the orchestrator (all exit 0):** `cargo test --workspace -- --test-threads=1` (1735 passed), clippy `-D warnings`, `cargo fmt --check`, `npm run check` (501 files, 0/0), `npm test` (4297 passed), `npm run build` (223 modules), bundle oracle correct, `cargo tree -p espansoconfig-core` has no tauri. Worker reports 15 failing-first mutations under `/private/tmp/4-12/`.
- **Risks to probe:**
  1. Byte preservation: does any new edit path (values items, option removal, remove-all, Form insertion, layout-only edits) send a draft that changes bytes outside its intent, or bypass `run_one_save` / the save gate / consent?
  2. `\r` handling: every new textarea/input refuses text holding `\r` at load, edit and send (CLAUDE.md §6).
  3. D2u: no inferred type shown; `multiline`/`default`/`trim_string_values` remain textual; no checkbox.
  4. Undo/history: compound operations (Add field, Form insertion) are one step; undo restores both halves.
  5. Reapply/conflict: `formsReapply` and the new buffers after a re-seed, a committed save, a conflict; stale buffers re-sent.
  6. Recovery refusal (`variablesNotCarried`) still covers snippets with forms; R36/R37 grant for structural presses.
  7. i18n: every new string EN+ES through typed accessors; no hand-built keys; the notes' reason for not using `describeFormEdit` (ruling 31).
  8. Any record/comment claiming a guarantee the code does not give.
- **Review file:** `docs/reviews/4-12.md`.
- **Time budget:** 15 minutes.
