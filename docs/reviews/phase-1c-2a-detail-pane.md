No High-severity findings.

## Medium

1. [DetailPane.svelte:124](/Users/ccarpio/Developer/espansoConfig/src/lib/components/DetailPane.svelte:124) — Unknown entries’ values are hidden while the UI claims they are shown

What is wrong: `unknownEntries` renders only `entry.key` and `entry.reason`. It never renders `value_kind`, `value_span`, or—most importantly—the value’s source text. Nevertheless, the displayed translations say the entry “is shown as written” ([en.json:61](/Users/ccarpio/Developer/espansoConfig/src/lib/i18n/en.json:61), [en.json:145](/Users/ccarpio/Developer/espansoConfig/src/lib/i18n/en.json:145)). The decision record repeats this claim at [1c-2a-notes.md:169](/Users/ccarpio/Developer/espansoConfig/docs/decisions/1c-2a-notes.md:169).

Failure scenario: an unmodelled entry equivalent to `custom_setting: 42` produces:

- the key `custom_setting`;
- a sentence saying it is shown as written;
- no `42`.

A mapping or sequence beneath the key disappears just as completely. This makes the detail pane an incomplete projection and prevents the reader from inspecting the content the app says it is showing.

Suggested fix: carry an exact source slice for unknown values across the Rust wire—byte slicing should remain in Rust rather than using JavaScript UTF-16 indices—and render it. Until that exists, change the wording to say the entry was “recorded and left untouched,” display `value_kind`, and document explicitly that its value is not displayed.

2. [DetailPane.svelte:86](/Users/ccarpio/Developer/espansoConfig/src/lib/components/DetailPane.svelte:86) — Sequence item boundaries are discarded visually

What is wrong: `detail.ts` models sequence items as `LineLabel { kind: 'item' }` and describes that label as a bullet ([detail.ts:211](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/detail.ts:211)), but the component renders only `key` and `unnamed` labels. It also disables native bullets with `list-style: none` at [DetailPane.svelte:380](/Users/ccarpio/Developer/espansoConfig/src/lib/components/DetailPane.svelte:380).

Failure scenario: two search terms where the first scalar contains a newline render as three unmarked text lines. The reader cannot distinguish two sequence items from three, even though `flattenValue()` preserved that structure. The same ambiguity affects `triggers`, `depends_on`, and nested variable parameters.

Suggested fix: render a structural marker whenever `line.label.kind === 'item'`, or retain nested list structure rather than flattening it without boundaries. Add a component-level assertion when component rendering becomes available; meanwhile, at least add a source scan specifically requiring the `item` branch.

## Low

3. [detail.test.ts:440](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/detail.test.ts:440) — The claimed field-coverage test derives its audit set from the implementation output

What is wrong: the test constructs an input containing every current field, but then audits only the rows and blocks that `describeMatch()` chose to emit. The fixed assertion is merely that the output contains 24 distinct names. Its comment claims that adding a field and forgetting it will change the count, which is false if the new field is omitted by `describeMatch()`.

Failure scenario: add a new `MatchView` field, its fixture override, `DetailFieldName`, and dictionary entry, but forget to add it to `describeMatch()`. The `produced` array remains the same 24 fields and the test passes.

Suggested fix: maintain an independently exhaustive expected field list with a type-level `Exclude<DetailFieldName, Expected[number]>`/reverse check, then compare the output set directly with that list. Derive expected presence from the constructed input, not from emitted rows.

4. [1c-2a-notes.md:26](/Users/ccarpio/Developer/espansoConfig/docs/decisions/1c-2a-notes.md:26) — The dictionary-change counts are incorrect and internally inconsistent

What is wrong: comparison with base commit `0507f6f` gives 49 keys added and one removed, taking the dictionaries from 169 to 217 keys. The record says “51 keys added” at lines 26 and 178, then “50 new Spanish values” at line 206.

Failure scenario: a later audit uses the record’s counts as its expected key delta and searches for two additions that do not exist, or treats an actual future drift as matching the recorded baseline.

Suggested fix: replace those claims with “49 keys added and one removed” and “49 new Spanish values,” ideally generated by a small read-only key-set comparison rather than counted manually.

The Spanish additions I spot-checked are idiomatic and internally consistent, including “Texto de sustitución.” I found no hardcoded user-facing prose, built translation keys in the component, D2u value interpretation, trigger/content collapsing, or corpus-privacy exposure. `flattenValue()` also cannot reach dangerous recursion through the real Rust projection because deeper nodes are capped and represented as `Elided`.

The Svelte component compiles directly with zero compiler warnings. Full `npm run check`/Vitest execution was blocked only because this environment is read-only and Vite attempted to create `node_modules/.vite-temp`; no files were modified.

Codex session ID: 019fb8f7-35c8-7d71-bcc2-285653aa1770
Resume in Codex: codex resume 019fb8f7-35c8-7d71-bcc2-285653aa1770
