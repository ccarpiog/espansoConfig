1. HIGH — [save.rs:1502](/Users/ccarpio/Developer/espansoConfig/crates/espansoconfig-core/src/persist/save.rs:1502), [validate/mod.rs:372](/Users/ccarpio/Developer/espansoConfig/crates/espansoconfig-core/src/validate/mod.rs:372), [save.rs:295](/Users/ccarpio/Developer/espansoConfig/crates/espansoconfig-core/src/persist/save.rs:295)  
   Concrete failure: consent for one candidate can commit a byte-different clone on a later revision. First request a duplicate of `matches[0]` while its same-length trigger is `:one` and retain the refused finding. Change that source trigger to same-length `:two`, obtain the new base revision, then submit the same duplicate with the old acknowledgement. The recomputed finding is still the unit code plus the same clone path, span and parser node: `duplicate_keeps_trigger_definition` carries no candidate revision or trigger operand, and `covers_all` compares only `Finding` equality. The old acknowledgement therefore covers the new finding and the `:two` clone commits without that candidate ever having been shown. This directly falsifies the doc comments' claim that path/span/node bind acknowledgement to one candidate and does not have `DocumentDoesNotParse`'s candidate digest protection.  
   Smallest fix: include the candidate `ContentRevision` in `DuplicateKeepsTriggerDefinition` (or otherwise in this finding's equality identity), update the wire/dictionary cascade, and add the two-revision, equal-length-trigger transfer test.

2. HIGH — [edit.rs:4535](/Users/ccarpio/Developer/espansoConfig/crates/espansoconfig-core/src/patch/edit.rs:4535), [edit.rs:4642](/Users/ccarpio/Developer/espansoConfig/crates/espansoconfig-core/src/patch/edit.rs:4642), [edit.rs:8163](/Users/ccarpio/Developer/espansoConfig/crates/espansoconfig-core/src/patch/edit.rs:8163)  
   Concrete failure: the verifier never independently checks that the zero-width arrival is at the derived boundary. The planner computes `point`, then authorizes and guards that same `point`; `the_arrival_is_the_copy` checks only that there is one zero-width replacement and that its text matches, while the digest/order and lockstep checks observe syntax rather than trivia placement. On `matches:\n  - trigger: a\n\n  - trigger: b\n`, a defective planner landing the exact clone after the separator blank instead of before it produces `a, blank, clone-a, b`. That candidate still parses as the intended item order, has the intended digests and comment multiset, changes no source byte, and writes the exact owned bytes, so every production verification property passes even though the blank separator now lies between source and clone and the design's derived immediate-after boundary was violated. This misses the design requirement at `phase-2c-3c-design.md:55` for an independently verified arrival at the derived boundary.  
   Smallest fix: re-derive the expected arrival offset in verification from the original item/text (not from `PlannedEdit`, `permitted`, or `DuplicateExpectation`'s planner claims), compare it to `arrival.span.start`, and add an internal adversarial verification test using the before/after-separator pair.

3. HIGH — [edit.rs:8153](/Users/ccarpio/Developer/espansoConfig/crates/espansoconfig-core/src/patch/edit.rs:8153), [edit.rs:8183](/Users/ccarpio/Developer/espansoConfig/crates/espansoconfig-core/src/patch/edit.rs:8183), [edit.rs:8338](/Users/ccarpio/Developer/espansoConfig/crates/espansoconfig-core/src/patch/edit.rs:8338), [2c-3c-1-notes.md:129](/Users/ccarpio/Developer/espansoConfig/docs/decisions/2c-3c-1-notes.md:129)  
   Concrete failure: the claimed independent bound does not prove that the planner's runs exclude a file-owned comment. It checks each claimed run only against the contiguous `item_own_lines` hull. Although `entry_owned_runs` independently derives the holes, its run list is used only to concatenate expected text; it is never compared to `expectation.runs`. The comment verifier then uses those same planner-supplied runs to decide which source comments deserve a copied occurrence and matches candidate comments globally by `(text, is_file)`, not by clone-relative identity. With identically spelled owned and file-owned comment lines in the same item hull, a defective run set can include the file-owned occurrence and omit the owned occurrence while producing identical concatenated bytes; node guards ignore comment/blank trivia, and the text/ownership multiset cannot distinguish the two equal spellings. The verifier can therefore certify copied provenance it was required to reject. The comments at `edit.rs:8124-8127` and the record at `2c-3c-1-notes.md:133-136` falsely claim every copied file comment necessarily fails the byte comparison.  
   Smallest fix: independently require the normalized claimed-run set to equal (or be exactly covered by, with no omissions from) `entry_owned_runs`'s run set, derive the copied-comment increment from that independent set, and verify clone-relative comment ownership rather than only a global text/class multiset. Add a mutation test with equal-spelling owned and file-owned comments.

4. LOW — [patch_duplicate.rs:918](/Users/ccarpio/Developer/espansoConfig/crates/espansoconfig-core/tests/patch_duplicate.rs:918), [patch_duplicate.rs:943](/Users/ccarpio/Developer/espansoConfig/crates/espansoconfig-core/tests/patch_duplicate.rs:943), [2c-3c-1-notes.md:207](/Users/ccarpio/Developer/espansoConfig/docs/decisions/2c-3c-1-notes.md:207)  
   Concrete failure: the real-corpus test cannot fail when every attempted duplicate regresses to a refusal, because every `Err` is unconditionally counted and accepted; it also skips parsed files with no located list and makes no assertion that any attempt or application occurred. Its success branch checks only a zero-width replacement and replays the engine's own replacement text, so it does not independently compare the inserted bytes to source-owned runs. The recorded run's `26 applied, 0 refused` is useful session evidence, but the record's stronger statement that every application passed "the independent byte checks" is not backed by this test, and a future all-refused regression remains green.  
   Smallest fix: make the synthetic mutation tests carry the independent proof, and make the real sweep non-vacuous when the corpus is present by asserting at least one eligible attempt/application and rejecting unexpected refusal classes (or pinning the recorded outcome census in an environment-specific evidence test).

## Verified without a finding

- `carve_envelope` is a behavior-preserving extraction from the previous `removal_envelope`: `git show HEAD:.../edit.rs` confirms the same `removal_span`, `preserved_regions`, `runs_between`, empty-run refusal, and the same three removal-only checks in the same order.
- The current planner's literal byte cases cover LF, CRLF, BOM, Unicode, mixed endings, first/middle/last items, file-comment holes, and both terminated and unterminated last-item EOF behavior. The EOF prefix is placed before the clone and uses `line_ending_before`; it never defaults to LF.
- The tested reachable refusals really reach their named arms: batch-only in both orders, `ArrivalLands`, `CopiedRunsJoin`, and EOF keep/clip refusal. Static tracing supports the record's claim that `ArrivalCloses` and the non-EOF kept-block arm are contradictory under the current substrate; `DuplicateWouldCopyAFileComment` is likewise unreachable while `carve_envelope` and `file_comment_inside` agree. `NoObservableLineEnding` remains unproved for a duplicate, as the record admits.
- Editor-model errors win in `verdict`; the triggerless duplicate test proves the duplicate suspicion is absent in that case. The English and Spanish finding text claims uncertainty only, neither language asserts collision semantics, and the Spanish additions use `fragmento` where they name a snippet and contain no `atajo`.
- The batch restriction is enforced beside the move restriction with the same `edits.len() != 1` strength. Public duplicate items are documented, the draft closed-surface audit rejects the new cardinality operation, and the public patch API does not itself write a file or bypass the transaction lock.
- The Rust/TypeScript wire shapes, `CODE_ENUMS`, `VARIANT_COUNTS`, sample totals and dictionary keys agree. The design is internally tense here: Q3 requires the frontend union cascade while Q7 step 1 literally says "No ... TypeScript"; because both cannot be obeyed and the review request expressly includes the cascade, I did not treat the type-only update as a finding.

## Checks run

- `git diff --check`
- `cargo test --workspace` — 1,036 passed, 0 failed
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo fmt --check`
- `npm test -- --run` — 1,244 passed, 0 failed
- `npm run check` — 0 errors, 0 warnings
- `npm run build` — 168 modules

READINESS: NOT READY

## Confirmation pass

1. LOW — [2c-3c-1-notes.md:329](/Users/ccarpio/Developer/espansoConfig/docs/decisions/2c-3c-1-notes.md:329), [2c-3c-1-notes.md:346](/Users/ccarpio/Developer/espansoConfig/docs/decisions/2c-3c-1-notes.md:346), [edit.rs:11535](/Users/ccarpio/Developer/espansoConfig/crates/espansoconfig-core/src/patch/edit.rs:11535), [edit.rs:11553](/Users/ccarpio/Developer/espansoConfig/crates/espansoconfig-core/src/patch/edit.rs:11553)  
   Concrete failure: the F3 disposition says the equal-spelling mutation is indistinguishable to the old byte layer and calls it the review's provenance mutation, but the mutation rebuilds the arrival from four ascending, disjoint spans in the order `trigger`, `first`, file-owned `# same note`, `second`. The independently derived honest bytes are `trigger`, owned `# same note`, `first`, `second`. Removing only the new run-set equality would therefore still refuse this mutation as `DuplicatedBytesWereRewritten`; the pre-existing byte oracle can distinguish it. The test currently reaches `DuplicateCarriesMoreThanTheItem` only because that check runs first, so it pins the new producer but does not establish the disposition's stronger claim that the old layers accepted the adversarial candidate. Section 6.3 also omits the necessary ascending/disjoint concatenation nuance instead of recording it without weakening the accepted proof gap. The production equality at [edit.rs:8261](/Users/ccarpio/Developer/espansoConfig/crates/espansoconfig-core/src/patch/edit.rs:8261) still closes F3, but the fix-round evidence and disposition overclaim what this mutation proves.  
   Smallest fix: leave the honest arrival bytes unchanged and tamper only `DuplicateExpectation.runs` plus the matching removal guard, using the equal-spelling owner swap. Then the old byte oracle sees the honest independent concatenation, while the new exact run-vector comparison is the first property that can reject the false provenance claim. State in §6.3 that ascending, disjoint source runs make the current reordered-byte mutation independently visible, while the accepted finding remains the missing equality between the planner's claim and the independently derived run set.

### Verified without another finding

- F1 is closed. `findings_of` hashes the post-edit candidate, `DuplicateKeepsTriggerDefinition` stores that `ContentRevision`, derived `Finding` equality compares the operand inside `Acknowledgement::covers_all`'s exact multiset, and the same-length `:one` to `:uno` transfer test proves path/span/node collide while the finding does not. An identical acknowledge-then-retry still commits; a second sequential duplicate produces different candidate bytes and therefore a different operand; batches cannot contain two duplicates.
- F2 is closed. `the_arrival_is_the_copy` re-derives `item_own_lines` from source text and the original node span and requires `arrival.span.start == lines.end`. The tampered helper moves the replacement, permitted span and insertion guard past the separator; its honest twin succeeds and the mutation reaches `DuplicateNotInPlace`.
- F3 is closed in production. The verifier compares the claimed ordered run vector with `entry_owned_runs`' independently derived vector, returns a `PlacedClone` containing only the independent boundary/prefix/runs, derives the comment increment from it, and checks clone-relative comment position and non-file ownership. The finding above is limited to the mutation's proof and the disposition's claim.
- F4 is closed. With the local corpus present the sweep attempts every third `matches` item, requires at least one attempt and application, rejects non-allowlisted errors (including every verification failure), and reparses each success to require exactly one additional match. The absent-corpus early return remains intact.
- The removal/move shared paths remain covered: the full workspace suite passes the removal, `RemoveItem`, `ItemMove`, structural-guard, corpus, and retained engine-mutation tests. No regression was found in `carve_envelope`/`removal_envelope` behavior.
- The wire declaration/sample parity tests read the Rust enums and confirm 189 total variants and `(115, 12, 62)` struct/newtype/unit shapes. The tagged TypeScript member carries `revision`; the operand-parity test sees it, every TypeScript consumer type-checks, and the generic variant-name/operand renderer accepts the tagged shape.
- The §6 corrections to §2.6, §2.8 and §3, and the corresponding production doc comments, accurately describe the candidate revision, derived arrival boundary, independent run/comment inputs, and the real sweep's narrower coverage role, apart from finding 1 above.

### Checks run

- `git diff --check`
- Focused F1/F2/F3/F4 tests, including the local real-corpus sweep
- `cargo test --workspace` — 1,040 passed, 0 failed
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo fmt --check`
- `npm test -- --run` — 1,244 passed, 0 failed
- `npm run check` — 0 errors, 0 warnings
- `npm run build` — 168 modules

READINESS: NOT READY
