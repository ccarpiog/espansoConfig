# Review brief — Phase 2d-6-10 (foreground fallback)

- **Repo:** /Users/ccarpio/Developer/Utils/espansoConfig (macOS Tauri v2 + Svelte 5 + TypeScript). Rules: `CLAUDE.md`.
- **Phase and goal:** 2d-6-10 per `docs/decisions/2d-6-split-notes.md` §2 block "2d-6-10 — foreground fallback", §5.8, §5.3, bound by §3 entries 33 and 38. Deliver a synchronous DOM `ForegroundSource` (`visibilitychange` to `visible` plus window `focus`) replacing `INERT_FOREGROUND_EVENTS` in `AppShell.svelte`; correct the coordinator's false asynchronous-Tauri comment; no permission or instrument hook changed. Acceptance: adapter tests; mounted shell cases (visible and focus each request a drain, hidden does not, triggers coalesce, unmount removes both listeners); a narrow foreground reading with wake and resume limits stated.
- **Changed files (uncommitted tree):** new `src/lib/browser/domForeground.ts`, `src/lib/browser/domForeground.test.ts`, `docs/decisions/2d-6-10-notes.md`, `docs/decisions/2d-6-10-window-reading.md`; modified `src/lib/components/AppShell.svelte`, `src/lib/components/AppShell.test.ts`, `src/lib/browser/reconciliationCoordinator.ts`, `src/lib/browser/workspace.svelte.ts` (comments), `src/lib/browser/reconciliationCoordinator.test.ts` (one comment).
- **Not in scope — the uncommitted window-reading instrument:** `src-tauri/src/main.rs`, `src/main.ts`, `src-tauri/src/probe.rs`, `src/probe.ts` are never committed (CLAUDE.md §6). Do not review them as product. `PROGRESS.json` is a workflow record.
- **Verification run by the orchestrator:** `npm test` exit 0, 3382 passed (70 files); `npm run check` exit 0, 459 files, 0 errors, 0 warnings; `npm run build` exit 0, 201 modules; server-only bundle markers absent, client-only present. No Rust changed.
- **Risks to probe:** (1) is the source truly synchronous in registration and removal, and does unsubscribe remove the exact listener functions; (2) does a `focus` plus a `visibilitychange` pair double-drain or coalesce as claimed, per the coordinator's contract; (3) does the corrected coordinator comment, and every other comment and record, claim only what the code forces (CLAUDE.md §5 last bullet — a false-guarantee comment is the worst defect class); (4) does the window reading overclaim — the screen was locked, no real foregrounding was observed, only a synthetic `focus`; check the notes and reading say so and do not mark window acceptance met beyond that; (5) mounted tests that would still pass with the inert source.
- **Review file:** `docs/reviews/phase-2d-6-10.md`.
- **Time budget:** 10 minutes.
