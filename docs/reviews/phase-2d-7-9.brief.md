# Review brief — Phase 2d-7-9: the owner-present visible session (G5), records only

- **Repo:** `/Users/ccarpio/Developer/Utils/espansoConfig` (branch `main`). Review the **uncommitted** tree.
- **Phase and goal:** 2d-7-9 per `docs/decisions/2d-7-split-notes.md` §2 *2d-7-9* — the owner at an unlocked screen, the G5 rows read where the frozen instrument can read them, the rest recorded unread under the owner's standing ruling (record §3 entry 37). Risk `high`. Records only: **no tracked source changed**.
- **How it was run:** interactively by `/autoclaude-opus` with the owner present; the orchestrator mediated the owner (Dock click, ⌘Q, the by-eye judgements), a worker wrote the records from the orchestrator's session log. 19 launches `G5-01` … `G5-19` through `launch-7.sh`, fresh bundle path each, language through the picker, no `:keepalive`, no substitution armed.
- **Changed files (the deliverables):** `docs/decisions/2d-7-9-notes.md`, `docs/decisions/2d-7-9-window-reading.md`. Workflow records also changed: `PROGRESS.md`, `PROGRESS.json`, this brief. The four instrument paths (`M src-tauri/src/main.rs`, `M src/main.ts`, `?? src-tauri/src/probe.rs`, `?? src/probe.ts`) are the standing uncommitted instrument, not this phase's change.
- **Primary evidence:** the launch directories under `/private/tmp/espansoconfig-harness-2d-6-6c-2/launches/G5-*/` (`probe.log`, `launch.txt`, `bytes.txt`, `shots/`), the orchestrator's session log at `/private/tmp/claude-501/-Users-ccarpio-Developer-Utils-espansoConfig/5d593df0-f70b-4e7b-a218-1482622b8ed6/scratchpad/g5-session.md`, and the unattended twins (G3-* and G4-* ran on an unlocked screen with every beat `visibility=visible`, so "hidden twin" is the consult's term, not a fact about them) `G3-01/15`, `G3-03/17`, `G3-07/21`, `G3-08/22`, `G4-01`, `G4-06`, `G4-02`, `G4-10`.
- **Verification run:** see the notes' verification block (the six `CLAUDE.md` §4 gates with the instrument present; expected rung `1330 / 462 / 3547 / 201`, unchanged; hash check `ok=18 diff=0` before and after).
- **What to check hardest (risks):**
  1. **Numbers against files.** Every beat time, drain tally, `alive-at-kill`, `lock-at-end`, `used=window` and pixel count quoted in the two records must match the launch files. Re-derive a sample.
  2. **Class discipline (entries 17, 18, 37).** No G5 row credited as read beyond what a transcript line or the owner's stated judgement supports; every unread row names its reason and the missing plan capability; the disabled-status-control look is on *hidden* 2d-7-8 snapshots and must not be written as a visible claim; the three visibility conditions are stated (or stated missing) at each claim; the Dock-click occlusion is 8.8–9.8 s, not ">10 s"; the visual-judgement launches ran at 1180x728 (the session log wrongly said 1080x728, and the records say so).
  3. **Attribution.** Each owner statement verbatim and the owner's; the orchestrator's own looks labelled as a model's reading. No wording change proposed (entry 26); open items go to 2d-7-10.
  4. **The foreground finding's scope.** The page not stopping under occlusion was read with a plan holding by IPC round trips; the records must not generalize it to timers or to the 1c-2b-2b-2 `setTimeout` stop.
  5. **Privacy (`CLAUDE.md` §1):** synthetic fixtures only; no real-config content anywhere.
- **Review file:** write the full report to `docs/reviews/phase-2d-7-9.md` (overwrite). Return only the verdict lines.
- **Time budget:** 10 minutes.
