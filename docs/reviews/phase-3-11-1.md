Reviewer: codex adversarial review

Codex adversarial review of the working tree at /Users/ccarpio/Developer/Utils/espansoConfig.
Codex verdict: needs-attention. 3 finding(s): 1 blocker(s), 2 should-fix.

Do not ship: Mixed comparison loses block-scalar headers, and bulk adoption has two reproducible state-ordering defects.

- [BLOCKER] high · confidence 1 — crates/espansoconfig-core/src/draft/bulk.rs:415 — Include block-scalar headers in exact spelling
    ScalarView.span excludes a block scalar’s header: SyntaxIndex::push_scalar assigns layout.content to node.span. Consequently, options written with identical bodies under `|` and `>…
    Fix: Derive the full spelling from Rust syntax/header spans, preserving the actual header bytes. Add accessor tests for identical bodies with dif…
- [SHOULD-FIX] medium · confidence 1 — src/lib/browser/workspace.svelte.ts:6955 — Invalidate every committed file before awaiting adoption
    After the result confirms that files A and B committed, this loop awaits A’s entire adoption before invalidating B. With getDocument(A) deferred, B’s old projection remains live an…
    Fix: Synchronously retire all committed files’ projections and identities before the first adoption await, then reread them. Test two committed f…
- [SHOULD-FIX] medium · confidence 1 — src/lib/browser/workspace.svelte.ts:8325 — Preserve newer selection intent during bulk rereads
    The new committed-bulk branch calls reprojectTheReplacedDocument, which captures the old selection before getDocument and restores it afterward without checking selectGeneration. R…
    Fix: Guard restoration and its notice with the selection generation captured after invalidation. Add deferred-read tests covering a newer selecti…

Scope: the working tree only — anything already committed for this phase was outside it.

VERDICT: ship-with-fixes
BLOCKERS: crates/espansoconfig-core/src/draft/bulk.rs:415 — Include block-scalar headers in exact spelling — Derive the full spelling from Rust syntax/header spans, preserving the actual header bytes. Add accessor tests for identical bodies with dif…
SHOULD-FIX: src/lib/browser/workspace.svelte.ts:6955 — Invalidate every committed file before awaiting adoption — Synchronously retire all committed files’ projections and identities before the first adoption await, then reread them. Test two committed f…
SHOULD-FIX: src/lib/browser/workspace.svelte.ts:8325 — Preserve newer selection intent during bulk rereads — Guard restoration and its notice with the selection generation captured after invalidation. Add deferred-read tests covering a newer selecti…
NOT-VERIFIED: Fix the spelling envelope and post-commit coordination, then add the targeted regression cases.
NOT-VERIFIED: Rerun the bulk accessor, bulk model, workspace and raw-save regression suites.
