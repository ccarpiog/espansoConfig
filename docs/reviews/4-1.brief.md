# Review brief — step 4-1 (A1 across creation and single-match editing)

- **Repo:** `/Users/ccarpio/Developer/Utils/espansoConfig` (uncommitted working tree against `HEAD`).
- **Phase and goal:** step 4-1 of Phase 4, specified in `docs/decisions/4-split-notes.md` §2 `### 4-1`
  (lines 85-123), with the rulings of §3 and the narrowings §4.2 and §4.3. Eight options (`word`,
  `left_word`, `right_word`, `propagate_case`, `uppercase_style`, `force_mode`, `force_clipboard`,
  `paragraph`) are written as validated plain source on *Create* and in the single-match editor; `anchor`
  stays a logical string; the core ambiguity exemption is widened only to the exact value nodes of
  requested `PlainSource` entries in inserted items. The worker's record is `docs/decisions/4-1-notes.md`.
- **Changed files:** `CLAUDE.md`; core `crates/espansoconfig-core/src/draft/{bulk,error,match_draft,new_match,plan}.rs`,
  `src/patch/edit.rs`, `src/patch/edit/corrupted_candidate_tests.rs`, `tests/{draft_compose,draft_plan,new_match_creation,persist_save}.rs`;
  `src-tauri/src/{commands.rs,commands/preservation_check.rs,dictionary_contract.rs,wire_contract.rs}`;
  `src/lib/browser/{creationDefaults,matchEditor}.test.ts`, `src/lib/i18n/{codes.ts,draftCodes.test.ts,en.json,es.json}`,
  `src/lib/ipc/types.ts`; new `docs/decisions/4-1-notes.md`. (`PROGRESS.json` is a workflow record.)
- **Verification run by the orchestrator (all exit 0):** `cargo test --workspace -- --test-threads=1`
  1567 passed / 0 failed; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo fmt --check`;
  `npm run check` 487 files 0/0; `npm test` 4076 passed; `npm run build` 214 modules; bundle oracle
  server-only absent, client-only present (2); `cargo tree -p espansoconfig-core` contains no tauri.
  Rung `1567 / 487 / 4076 / 214` against `1555 / 487 / 4074 / 214`.
- **Risks to probe hardest:**
  1. The exemption width in `patch/edit.rs` (`plain_source_nodes` or its successor): is it exactly the
     requested entries' value nodes of inserted items — not keys, not siblings, not list elements, not
     neighbouring values — including under several inserted items and index shifts? Could a mismatch
     between how the exemption locates the node and how the candidate is reparsed exempt the wrong node?
  2. `Unchanged` vs `Set` in `plan_scalar`: an untouched quoted `'true'` must survive byte-identically, and
     typing then undoing back to the baseline must stay `Unchanged`. The worker changed ~15 existing test
     expectations (notably "every field set to its own value derives nothing") — check each change is a
     consequence of the ruling, not a weakened assertion.
  3. Refusal before any transaction on both paths for non-plain-source text (empty, line break, quote,
     `#`, flow indicator, alias, tag); refusal by name; the empty stored sidecar default stays visible and
     removable in the creator and is refused at *Create*, never dropped or quoted; sidecar bytes/schema
     unchanged.
  4. Wire/i18n: `DraftError::OptionNotPlainSource` travels inside `draftRefused`; check the IPC mirror,
     `wire_contract.rs`, `dictionary_contract.rs`, both dictionaries, the `codes.ts` accessor and its
     reactive wrapper agree, and that the Spanish sentence is Spanish.
  5. Records: `4-1-notes.md` and the `CLAUDE.md` edit claim nothing the code does not force (the project's
     worst defect class). Failing-first evidence is recorded.
- **Review file:** `docs/reviews/4-1.md`.
- **Time budget:** 20 minutes.
