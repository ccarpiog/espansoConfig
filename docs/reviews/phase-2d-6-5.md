Reviewer: codex adversarial review

Codex adversarial review of the working tree at /Users/ccarpio/Developer/Utils/espansoConfig.
Codex verdict: needs-attention. 4 finding(s): 2 blocker(s), 2 should-fix.

Do not ship. Read-only probes reproduced two submission-block failures, stale conflict restoration during reload, and a false candidate-retention disclosure. No files were modified.

- [BLOCKER] high · confidence 1 — src/lib/browser/rawEditor.ts:906 — [BLOCKER] Raw save checks the installed session too early
    After current() is checked, canSave reads draft.value again, submissionOf reads it again, and the session spread performs further caller-controlled reads. A getter firing on the th…
    Fix: Capture the submission, eligibility result, and successor before the final installed-session check. Validate the exact captured submission c…
- [BLOCKER] high · confidence 1 — src/lib/browser/restore.ts:2410 — [BLOCKER] Restore preparation erases observations on early refusal
    The early refusal returns bypass current(). Reproduced with a candidate-bearing session and context.observed getter that delivers a superseding observation through BrowserState, th…
    Fix: Consult the installed-session reader on every return path, including refusals after preview, context, and submission reads. Return the live…
- [SHOULD-FIX] medium · confidence 1 — src/lib/browser/restore.ts:3689 — [SHOULD-FIX] Reload results overwrite a superseding conflict
    Reproduced on both surfaces through the real adoptDiskVersion: confirm observation A, then let A.disk.id publish observation B while adoption copies the projection. The receiver in…
    Fix: Add installed-session readers to both reload transitions. After adoption and all successor-building reads, preserve the latest installed ses…
- [SHOULD-FIX] medium · confidence 0.99 — src/lib/browser/restore.ts:3952 — [SHOULD-FIX] Empty restore state claims a selected candidate exists
    startRestore followed by a raised observation produces preview=null and pending=null, but this placeholder construction uses the ordinary candidate-retaining capabilities. Conseque…
    Fix: Make the exposed messages reflect whether a real preview and confirmation exist. Suppress candidate-dependent claims for the empty state whi…

Scope: the working tree only — anything already committed for this phase was outside it.

VERDICT: ship-with-fixes
BLOCKERS: src/lib/browser/rawEditor.ts:906 — [BLOCKER] Raw save checks the installed session too early — Capture the submission, eligibility result, and successor before the final installed-session check. Validate the exact captured submission c…
BLOCKERS: src/lib/browser/restore.ts:2410 — [BLOCKER] Restore preparation erases observations on early refusal — Consult the installed-session reader on every return path, including refusals after preview, context, and submission reads. Return the live…
SHOULD-FIX: src/lib/browser/restore.ts:3689 — [SHOULD-FIX] Reload results overwrite a superseding conflict — Add installed-session readers to both reload transitions. After adoption and all successor-building reads, preserve the latest installed ses…
SHOULD-FIX: src/lib/browser/restore.ts:3952 — [SHOULD-FIX] Empty restore state claims a selected candidate exists — Make the exposed messages reflect whether a real preview and confirmation exist. Suppress candidate-dependent claims for the empty state whi…
NOT-VERIFIED: Add targeted regressions for these failures, fix the transitions and false guarantees, then rerun frontend checks.
NOT-VERIFIED: Checked and did not hold: nested delivery loss through BrowserState, which serializes delivery; placeholder submission/copy; consent reuse across normal reseed/…
NOT-VERIFIED: do-not-ship BLOCKERS: 2 SHOULD-FIX: 2
