Reviewer: codex adversarial review

Codex adversarial review of the working tree at /Users/ccarpio/Developer/Utils/espansoConfig.
Codex verdict: needs-attention. 4 finding(s): 0 blocker(s), 4 should-fix.

Do not ship yet: unreadable declarations and settings can produce false dependency findings, global usage counts are lost, and malformed layouts cause quadratic work inside the save path.

- [SHOULD-FIX] medium · confidence 0.99 — crates/espansoconfig-core/src/analysis/dependency.rs:452 — Unreadable inject_vars is treated as certainly enabled
    For `inject_vars: *flag` or `inject_vars: {}`, projection leaves `variable.inject_vars` as None and records an unknown entry. This call interprets None as an absent setting and ret…
    Fix: Distinguish absent inject_vars from an unreadable written value before applying the default. Classify unreadable values as Uncertain, omit i…
- [SHOULD-FIX] medium · confidence 0.98 — crates/espansoconfig-core/src/analysis/dependency.rs:417 — Partially unreadable declarations still produce closed-scope findings
    The scope checks only whether the outer variable containers were projected. A global variable with `name: *declared_name` and a readable type is projected without a name, yet the m…
    Fix: Make dependency-analysis scope closure account for unreadable local and global declaration names, propagate the uncertainty reason, and supp…
- [SHOULD-FIX] medium · confidence 1 — crates/espansoconfig-core/src/analysis/dependency.rs:339 — Global-to-global usage counts are overwritten
    The global variables pass resolves other globals as Local and increments usage on globals.declarations. The separate global_usage vector receives only references from matches. Assi…
    Fix: Accumulate match usage into the existing declaration counts instead of replacing them. Test global-to-global references both with and withou…
- [SHOULD-FIX] medium · confidence 1 — crates/espansoconfig-core/src/analysis/placeholder.rs:114 — Repeated malformed openers make placeholder parsing quadratic
    For a layout containing repeated `[[` without `]]`, every iteration searches the entire remaining suffix for a closer, then advances only two bytes. This performs quadratic work de…
    Fix: Scan forward once for the next opener or closer, or retain delimiter positions across iterations. Add a large repeated-opener regression tha…

Scope: the working tree only — anything already committed for this phase was outside it.

VERDICT: ship-with-fixes
BLOCKERS: none
SHOULD-FIX: crates/espansoconfig-core/src/analysis/dependency.rs:452 — Unreadable inject_vars is treated as certainly enabled — Distinguish absent inject_vars from an unreadable written value before applying the default. Classify unreadable values as Uncertain, omit i…
SHOULD-FIX: crates/espansoconfig-core/src/analysis/dependency.rs:417 — Partially unreadable declarations still produce closed-scope findings — Make dependency-analysis scope closure account for unreadable local and global declaration names, propagate the uncertainty reason, and supp…
SHOULD-FIX: crates/espansoconfig-core/src/analysis/dependency.rs:339 — Global-to-global usage counts are overwritten — Accumulate match usage into the existing declaration counts instead of replacing them. Test global-to-global references both with and withou…
SHOULD-FIX: crates/espansoconfig-core/src/analysis/placeholder.rs:114 — Repeated malformed openers make placeholder parsing quadratic — Scan forward once for the next opener or closer, or retain delimiter positions across iterations. Add a large repeated-opener regression tha…
NOT-VERIFIED: Add focused regressions for these four cases, fix the analysis, and rerun the relevant core tests and workspace checks.
