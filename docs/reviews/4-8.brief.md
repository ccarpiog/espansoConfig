# Review brief — step 4-8 (revision-bound snapshots and command integration)

- **Repo:** /Users/ccarpio/Developer/Utils/espansoConfig — review the **uncommitted** working tree against `HEAD` (`250f1be`).
- **Phase and goal:** step 4-8 of Phase 4, defined in `docs/decisions/4-split-notes.md` §2 `### 4-8`: Rust-cut authoring snapshots, candidate analysis on the wire, the widened `save_match`, and a variable-reorder writer that ends in `run_one_save`. Acceptance: stale identities and revisions refuse (D2v); a submitted span is never trusted (R28); every writer reaches `run_one_save` and none takes a second lock; an atomic content-plus-variable save, an acknowledgement round trip, a no-op and an uncertain outcome are tested on temporary synthetic files. Risk **high**. No visible UI caller yet.
- **The worker's record:** `docs/decisions/4-8-notes.md` (decisions, guarantees, open items).
- **Changed files:** new `crates/espansoconfig-core/src/authoring.rs`, `crates/espansoconfig-core/tests/authoring_snapshots.rs`, `src-tauri/src/commands/authoring_check.rs`, `src/lib/i18n/analysisCodes.test.ts`; modified `crates/espansoconfig-core/src/{lib.rs,persist/mod.rs,persist/save.rs,analysis/mod.rs,analysis/dependency.rs,analysis/placeholder.rs}`, `crates/espansoconfig-core/tests/dependency_analysis.rs`, `src-tauri/src/{commands.rs,error.rs,main.rs,wire_contract.rs,dictionary_contract.rs,dispatch_check.rs}`, `src/lib/ipc/{types.ts,commands.ts,errors.ts,commands.test.ts}`, `src/lib/i18n/{codes.ts,index.ts,codes.test.ts,en.json,es.json}`. (`PROGRESS.json` is a workflow record; ignore it.)
- **Verification run by the orchestrator, all exit 0:** `cargo test --workspace -- --test-threads=1` (1726 passed, 0 failed); `cargo clippy --workspace --all-targets -- -D warnings`; `cargo fmt --check`; `npm run check` (488 files, 0 errors, 0 warnings); `npm test` (4092 passed); `npm run build` (214 modules); bundle oracle correct; `cargo tree -p espansoconfig-core` holds no tauri.

## Risks to probe

1. **Single-writer / lock invariants (CLAUDE.md §6):** does the new `move_variable` writer and the widened `save_match` reach `run_one_save` exactly once, with no second lock, and no call to `replace_file_atomically`/`replace_locked_file` outside `save_document`? Is the `SaveTail` closure seam (`commands.rs` ~2429) something production can only fill with the real tail, or can a production path bypass `run_one_save` through it?
2. **`analyze_match_candidate` takes no lock and writes nothing** — confirm it cannot write, cannot mutate the workspace cache, and does not leak byte spans or config content to the wire.
3. **D2v / R28:** stale `MatchId`/revision refused on every new command (handled, never unwrapped); no deserialized `ByteSpan` reaches the patch engine unrevalidated.
4. **A committed write is never reported as an error** — the notes say the uncertain outcome is reported as an error "the file may have changed"; check that this matches the existing writers' policy for an *uncertain* (not committed) outcome and does not report a known-committed write as an error.
5. **Snapshot fingerprints:** a container's text covers its value only (notes open item); is anything claimed about it that the code does not give?
6. **Changes to `analysis/dependency.rs`, `placeholder.rs`, `persist/save.rs`:** any behaviour change to 4-7's findings or the save path beyond what the notes claim?
7. **i18n:** every new code has EN+ES strings through typed accessors in `codes.ts`; no hand-built keys; no hardcoded user-facing string.
8. **Records claiming guarantees the code does not give** (the project's worst defect class) — in the notes, doc comments and TS comments.

## Output

- **Review file:** `docs/reviews/4-8.md` (verdict `ship` / `ship-with-fixes` / `do-not-ship`, BLOCKERS and SHOULD-FIX lists with file:line).
- **Time budget:** 20 minutes.
- Never quote real espanso config content (the gitignored `tests/corpus/real/`).
