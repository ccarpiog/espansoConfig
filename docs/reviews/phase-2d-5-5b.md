Reviewer: codex adversarial review

Codex adversarial review of the working tree at /Users/ccarpio/Developer/Utils/espansoConfig.
Codex verdict: needs-attention. 4 finding(s): 3 blocker(s), 1 should-fix.

do-not-ship — three source blockers and one source SHOULD-FIX reproduced with in-memory checks. No files changed; supplied full-suite gates were not rerun.

- [BLOCKER] high · confidence 0.99 — src/lib/browser/workspace.svelte.ts:2960 — BLOCKER (source): retained observations acquire a falsely fresh generation
    A save commits revision C while observation D is retained. Before settlement, the save's required refresh reads and installs newer revision E. Releasing D then registers it against…
    Fix: Preserve the observation's arrival generation through retention and registration. Refuse installation after an intervening projection replac…
- [BLOCKER] high · confidence 1 — src/lib/browser/workspace.svelte.ts:2878 — BLOCKER (source): re-entrant arbitration overwrites a newer origin
    arbitrateHere captures the standing origin before standingConflictOf and arbitrateObservation read caller-controlled properties, then registers their verdict without rechecking sta…
    Fix: Snapshot caller-controlled operands before the final state checks. Revalidate standing origin, workspace generation and write barrier immedi…
- [BLOCKER] high · confidence 1 — src/lib/browser/workspace.svelte.ts:4871 — BLOCKER (source): write leases and outcomes are not exception-safe
    All six wrappers acquire a lease without unconditional cleanup. A command rejection or an exception during reporting/reprojection bypasses settle. Reproduced for every wrapper: wri…
    Fix: Use an exception-safe lease lifecycle across all six wrappers, retaining the known settlement and conservatively marking unknown outcomes un…
- [SHOULD-FIX] medium · confidence 1 — src/lib/browser/reapply.ts:470 — SHOULD-FIX (source): the standing operand becomes stale during evidence reads
    Even an honest caller passing state.standingConflictFor(document) receives no live supersession check. After the identity comparison, correspondence, draft and iterator reads can r…
    Fix: Snapshot evidence, including array iteration, before checking live standing state through a state-owned guard. Return superseded when those…

Scope: the working tree only — anything already committed for this phase was outside it.

VERDICT: ship-with-fixes
BLOCKERS: src/lib/browser/workspace.svelte.ts:2960 — BLOCKER (source): retained observations acquire a falsely fresh generation — Preserve the observation's arrival generation through retention and registration. Refuse installation after an intervening projection replac…
BLOCKERS: src/lib/browser/workspace.svelte.ts:2878 — BLOCKER (source): re-entrant arbitration overwrites a newer origin — Snapshot caller-controlled operands before the final state checks. Revalidate standing origin, workspace generation and write barrier immedi…
BLOCKERS: src/lib/browser/workspace.svelte.ts:4871 — BLOCKER (source): write leases and outcomes are not exception-safe — Use an exception-safe lease lifecycle across all six wrappers, retaining the known settlement and conservatively marking unknown outcomes un…
SHOULD-FIX: src/lib/browser/reapply.ts:470 — SHOULD-FIX (source): the standing operand becomes stale during evidence reads — Snapshot evidence, including array iteration, before checking live standing state through a state-owned guard. Return superseded when those…
NOT-VERIFIED: Fix the three blockers and add regression tests covering the reproduced failures.
NOT-VERIFIED: Correct the phase record's claim that its acknowledged limitations contain no unfixed source defects.
NOT-VERIFIED: Run the affected frontend suites and checks after the fixes.
