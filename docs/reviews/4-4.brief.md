# Review brief — step 4-4 (local variable insertion, deletion and movement)

- **Repo:** `/Users/ccarpio/Developer/Utils/espansoConfig` — review the **uncommitted** working tree against `HEAD` (`6e615f6`).
- **Phase and goal:** step 4-4 of Phase 4, spec in `docs/decisions/4-split-notes.md` §2 `### 4-4` (and the §3 rulings, especially ruling 7 on author keys and ruling 4 on plain-source typed settings). The step delivers a closed `NewVariable` description (nine kinds, bounded extra author-named params), insertion into an existing `vars` and of a whole `vars:` subtree when absent, item removal (last-item removal leaves an explicit empty list or goes through an explicit container removal, never an unnoticed null), and a same-sequence reorder alone in its batch (R25 / `MoveMustBeTheOnlyEditInItsBatch`). Core-first, no production UI caller. Implementer's record: `docs/decisions/4-4-notes.md`.
- **Changed files:** new `crates/espansoconfig-core/src/draft/new_variable.rs`, `crates/espansoconfig-core/tests/variable_intents.rs`, `docs/decisions/4-4-notes.md`; modified `crates/espansoconfig-core/src/draft/{audit,error,match_draft,mod,plan}.rs`, `crates/espansoconfig-core/src/patch/{edit.rs,mod.rs,edit/corrupted_candidate_tests.rs}`, `crates/espansoconfig-core/tests/{draft_plan,draft_sequence}.rs` (two expectations changed: whole-variable / whole-`vars` removal now allowed), `src-tauri/src/{dictionary_contract,wire_contract}.rs`, `src/lib/ipc/types.ts`, `src/lib/browser/matchEditor.ts`, `src/lib/i18n/{en,es}.json`, `src/lib/i18n/draftCodes.test.ts`, three TS test fixtures. `PROGRESS.json` is a workflow record — ignore it.
- **Verification run by the orchestrator (all exit 0):** `cargo test --workspace -- --test-threads=1` 1625 passed / 0 failed; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo fmt --check`; `npm run check` 487 files 0/0; `npm test` 4086 passed; `npm run build` 214 modules; bundle oracle correct; `cargo tree -p espansoconfig-core` holds no tauri.
- **Risks to probe:**
  1. Byte preservation: does anything outside the intended span change on insertion (into an existing `vars`, and a new `vars:` subtree), removal and reorder — over block scalars, comments (item-owned vs file-owned), blank-line runs, CRLF, flow-style `vars`?
  2. Verifiers: do the new expectations/verifiers for nested shapes actually catch a wrong rendering (the corrupted-candidate tests), or can a mis-rendered nested value pass?
  3. R25: is a variable reorder refused beside **every** other edit category, including a match-level move, a params edit and a vars insertion/removal? Is the check-before-spend order right?
  4. Emission: are typed settings (`inject_vars`, `offset`, `trim`, `debug`) written as validated plain source and never as a quoted string that changes meaning (ruling 4, D2u), and are all other values quoted only as needed, never introducing a YAML 1.1-ambiguous plain scalar?
  5. Refusal hygiene: no `DraftError` carries key/value text; each new code has EN and ES strings; the audit surface widened only by named shapes.
  6. Last-item removal can never leave `vars:` null; the two changed test expectations are justified.
  7. Records: does `4-4-notes.md` claim any guarantee the code does not give (this project's worst defect class)?
- **Review file:** `docs/reviews/4-4.md`.
- **Time budget:** 15 minutes.
