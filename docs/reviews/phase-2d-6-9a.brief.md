# Review brief — Phase 2d-6-9a

- **Repo:** /Users/ccarpio/Developer/Utils/espansoConfig (macOS Tauri v2 app; Rust core + Svelte 5/TypeScript frontend). Rules: `CLAUDE.md`.
- **Phase and goal:** 2d-6-9a — the reconciliation-status presentation decisions as values, and their words. Scope: the 2d-6-9a bullet of *"The orchestrator's cut of 2d-6-9"* in `docs/decisions/2d-6-split-notes.md` §2 (below `### 2d-6-9`). A `src/lib/browser/` module deciding which of ten states (stale, unavailable, removed, path drift, not watched, failed registration, lost history, membership reload wanted, held observation, uncertain write) applies to the workspace and each file, and which of five controls (membership reload, lost-history exit, stale-file reread, retry, uncertainty acknowledgement) is offered/enabled; EN/ES keys with typed accessors; model tests incl. empty-workspace retention; rulings (2)/(3) and `2d-6-8c-notes.md` §4 item 4 decided. **No component changes.** Bound by split-record §3 entries 13-18, 26-32, 34-36, 38-40 and corrections §5.2, §5.4-§5.6.
- **Changed files (the phase's deliverables):** new `src/lib/browser/reconciliationStatus.ts`, `src/lib/browser/reconciliationStatus.test.ts`, `src/lib/i18n/reconciliationStatusCodes.test.ts`, `docs/decisions/2d-6-9a-notes.md`; modified `src/lib/i18n/{en,es}.json`, `codes.ts`, `index.ts`, `externalConflictCodes.test.ts`; comment-only `src/lib/browser/{workspace.svelte.ts,reconciliationCoordinator.ts,observationTransitions.ts}`; the cut itself in `docs/decisions/2d-6-split-notes.md`.
- **NOT part of this phase — ignore:** `src-tauri/src/main.rs`, `src/main.ts`, `src-tauri/src/probe.rs`, `src/probe.ts` (the deliberately uncommitted window-reading instrument), and `PROGRESS.json`.
- **Verification run:** `npm run check` exit 0 (452 files, 0 errors, 0 warnings); `npm test` exit 0 (3212 passed, 67 files); `npm run build` exit 0 (194 modules). No Rust changed. Rung `1323 / 449 / 3156 / 193` → `1323 / 452 / 3212 / 194`.
- **Risks to probe:**
  1. Does each decision derive only from state the frontend can actually observe (§5.4), and does it agree with what the corresponding request/press would actually do (refusal codes in the request's own order)? A decision that says "enabled" where the press refuses, or vice versa, is a defect.
  2. Are all ten states and five controls covered, with the three uncertainty exits (§5.5) and the retry release (§5.6)? Empty-workspace retention (§5.2)?
  3. False-guarantee comments/records (CLAUDE.md §5): any sentence in the module, the corrected comments or the notes that claims more than the code forces.
  4. The ruling on `2d-6-8c-notes.md` §4 item 4 (claimed deliberate: the refused save marks disk state under the session lock, the watcher's later reading coalesces as `Admission::Duplicate`, pinned near `ledger.rs:3552`) — is the diagnosis right? And the handed-on observation that a file is never marked `stale` after the save panel closes without loading the disk version.
  5. i18n: real Spanish, key/placeholder parity, accessors never hand-built keys; the change to `externalConflictCodes.test.ts` (count 7 → 9) does not weaken the guard.
- **Review file:** `docs/reviews/phase-2d-6-9a.md`
- **Time budget:** 15 minutes.
