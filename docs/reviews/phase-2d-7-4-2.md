Reviewer: codex adversarial review

Codex adversarial review of the working tree at /Users/ccarpio/Developer/Utils/espansoConfig.
Codex verdict: needs-attention. 5 finding(s): 2 blocker(s), 3 should-fix.

Do not freeze this instrument yet. Read-only reproductions exposed false reconciliation and witness results, and retained plans lack required harness setup. Hashes match; no app was launched.

- [BLOCKER] high · confidence 0.99 — /Users/ccarpio/Developer/Utils/espansoConfig/src/probe.ts:5077 — A failed fetch can conceal an unrecorded fallback dispatch
    pageCounts counts transport-failed records as ordinary commands. Tauri's pinned ipc-protocol.js retries a rejected fetch through postMessage. If the fetch failed before dispatch, t…
    Fix: Permanently mark reconciliation VOID after any transport-failed or unreadable record, independently of count equality. Add a deterministic p…
- [BLOCKER] high · confidence 1 — /private/tmp/espansoconfig-harness-2d-6-6c-2/launch-7.sh:338 — The unified harness cannot run several retained plans
    Every plan receives the same LF successor fixtures, and build_tree seeds no backup batch. Consequently external-restore, external-restore-cr, external-restore-drop and restore-save…
    Fix: Restore manifest-verified fixture selection per case and seed the required private backup batches before taking xdg-before. Verify each reta…
- [SHOULD-FIX] medium · confidence 0.99 — /Users/ccarpio/Developer/Utils/espansoConfig/src/probe.ts:5119 — Incomplete witnesses can certify an unchanged tree
    readWitness discards Rust's roots[].state and unlisted diagnostics, and returns truncated witnesses as usable maps. Rust can return entries=[] with a root marked 'not walked', with…
    Fix: Preserve completeness diagnostics and classify spans as unread or VOID whenever either witness has an unwalked root, unlisted subtree, or tr…
- [SHOULD-FIX] medium · confidence 0.97 — /Users/ccarpio/Developer/Utils/espansoConfig/src/probe.ts:5227 — A delivery during the tally read can spuriously void a launch
    Rust's emitted count is sampled before the awaited tally response, but spyDeliveries is sampled afterward. The retry guard checks only command activity. A wake delivered between th…
    Fix: Capture the delivery counter before requesting the tally and retry if it changes during the read, alongside the existing command guards.
- [SHOULD-FIX] medium · confidence 1 — /Users/ccarpio/Developer/Utils/espansoConfig/src/probe.ts:5888 — Open item: retained plans lack per-path no-write evidence
    Outside the entry-7 blockers: retained external/status plans receive only the launch-wide start/end checkpoints. Their probe writers record no post-write witness, and the per-write…
    Fix: Inventory the required rows, add action-boundary checkpoints and immediate probe-writer witnesses, and resolve missing plans before relying…

Scope: the working tree only — anything already committed for this phase was outside it.

VERDICT: ship-with-fixes
BLOCKERS: /Users/ccarpio/Developer/Utils/espansoConfig/src/probe.ts:5077 — A failed fetch can conceal an unrecorded fallback dispatch — Permanently mark reconciliation VOID after any transport-failed or unreadable record, independently of count equality. Add a deterministic p…
BLOCKERS: /private/tmp/espansoconfig-harness-2d-6-6c-2/launch-7.sh:338 — The unified harness cannot run several retained plans — Restore manifest-verified fixture selection per case and seed the required private backup batches before taking xdg-before. Verify each reta…
SHOULD-FIX: /Users/ccarpio/Developer/Utils/espansoConfig/src/probe.ts:5119 — Incomplete witnesses can certify an unchanged tree — Preserve completeness diagnostics and classify spans as unread or VOID whenever either witness has an unwalked root, unlisted subtree, or tr…
SHOULD-FIX: /Users/ccarpio/Developer/Utils/espansoConfig/src/probe.ts:5227 — A delivery during the tally read can spuriously void a launch — Capture the delivery counter before requesting the tally and retry if it changes during the read, alongside the existing command guards.
SHOULD-FIX: /Users/ccarpio/Developer/Utils/espansoConfig/src/probe.ts:5888 — Open item: retained plans lack per-path no-write evidence — Inventory the required rows, add action-boundary checkpoints and immediate probe-writer witnesses, and resolve missing plans before relying…
NOT-VERIFIED: Fix the blocking instrument defects within 2d-7-4 and verify the failure paths deterministically.
NOT-VERIFIED: Rerun affected shakedowns after fixes, then refresh the reviewed copy and recorded hashes.
