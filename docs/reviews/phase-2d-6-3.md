Reviewer: codex adversarial review

Codex adversarial review of the working tree at /Users/ccarpio/Developer/Utils/espansoConfig.
Codex verdict: needs-attention. 2 finding(s): 2 blocker(s), 0 should-fix.

Do not ship: two reproduced paths discard submission blocks. Suspicions about spending RecoveryOrigin, writtenHere lifting another observation’s wait, and anchor fallback did not hold.

- [BLOCKER] high · confidence 1 — src/lib/browser/recovery.ts:2237 — Recovery send settlement discards deliveries received during the request
    sendRecoveryCreate settles started.session, captured before await, rather than the installed session updated by applyRecoveryObservation. Reproduced through BrowserState: start rec…
    Fix: Settle every answer against the current installed session, preserving and replaying its heldDeliveries. Add a regression through sendRecover…
- [BLOCKER] high · confidence 1 — src/lib/browser/matchCreation.ts:1044 — Changing destinations permanently erases unresolved waits
    With A chosen, receive retained(A), choose B, then choose A again. Choosing B deletes the only record of A’s wait, and returning to A restores nothing. Both beginCreate and beginRe…
    Fix: Preserve pending observations by document across destination changes, or synchronously restore the destination’s authoritative pending state…

Scope: the working tree only — anything already committed for this phase was outside it.

VERDICT: ship-with-fixes
BLOCKERS: src/lib/browser/recovery.ts:2237 — Recovery send settlement discards deliveries received during the request — Settle every answer against the current installed session, preserving and replaying its heldDeliveries. Add a regression through sendRecover…
BLOCKERS: src/lib/browser/matchCreation.ts:1044 — Changing destinations permanently erases unresolved waits — Preserve pending observations by document across destination changes, or synchronously restore the destination’s authoritative pending state…
SHOULD-FIX: none
NOT-VERIFIED: Fix both paths and add the reproduced interleavings to the regression suites. Validation used in-memory probes against the actual modules and BrowserState; no f…
NOT-VERIFIED: Verdict: do-not-ship BLOCKERS: 2 SHOULD-FIX: 0
