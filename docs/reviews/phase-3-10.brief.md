# Review brief — Phase 3-10, the per-file bulk coordinator

- **Repo:** `/Users/ccarpio/Developer/Utils/espansoConfig` (macOS Tauri v2; Rust core `crates/espansoconfig-core`, shell `src-tauri`, Svelte/TS frontend `src/`). Project rules: `CLAUDE.md` (§3, §5, §6 "Writing files" are binding).
- **Phase and goal:** 3-10 per `docs/decisions/3-split-notes.md` §2 step "3-10" and rulings 19–22. One backend bulk command (`apply_bulk_options`) that preflights every file, keeps consent per file (base revision, candidate, exact findings multiset), runs one `run_one_save` per file in sequence, stops at the first conflict, refusal, failure or uncertain write, and accounts for every file (saved / already unchanged / refused or conflicted / write outcome unknown / not attempted / excluded before apply). Allowed fields: `word`, `left_word`, `right_word`, `propagate_case`, `uppercase_style`, `force_mode`, `force_clipboard`. No UI (that is 3-11).
- **Acceptance:** several matches and absent options share one save per file; a preflight blocker writes nothing; an injected second-file failure keeps the first file's success and reports it; an uncertain write stops later attempts; each file's actual backup or no-op result is visible; no `force` flag, and no path lock held around `run_one_save`.
- **Changed files (uncommitted):** new `crates/espansoconfig-core/src/draft/bulk.rs`, `crates/espansoconfig-core/tests/draft_bulk.rs`, `src-tauri/src/bulk.rs`, `src-tauri/src/commands/bulk_check.rs`, `src/lib/i18n/bulkCodes.test.ts`, `docs/decisions/3-10-notes.md`; modified core `draft/mod.rs`, `persist/save.rs` (new `preflight_edits`), `persist/mod.rs`; `src-tauri/src/{commands,error,main,dictionary_contract,wire_contract,dispatch_check}.rs`; `src/lib/ipc/{types,errors,commands,commands.test}.ts`; `src/lib/i18n/{codes,index,codes.test}.ts`, `en.json`, `es.json`. `PROGRESS.json` is a workflow record — ignore it.
- **Verification run (orchestrator, all exit 0):** `cargo test --workspace -- --test-threads=1` 1491 passed; clippy `-D warnings`; `cargo fmt --check`; `cargo tree -p espansoconfig-core | rg tauri` empty; `npm run check` 475 files 0/0; `npm test` 3833; `npm run build` 207 modules; bundle oracle correct.
- **Risks to probe hardest:**
  1. Does preflight really write nothing when any file blocks, and can the preflight verdict differ from what `run_one_save` then does (TOCTOU between preflight and save — is the base revision re-checked)?
  2. Is `run_one_save` called without any lock held by the coordinator (the lock is not reentrant — a hang)? Is `save_document` still the only writer?
  3. Consent: is acknowledgement matched per file as an exact multiset against that file's own candidate, so consent for one file/draft cannot be spent on another?
  4. Outcome accounting: is every requested file given exactly one outcome; is a committed write ever reported as an error; is "nothing written" claimed only when true for the whole request; is "write outcome unknown" distinguished from a refusal and does it stop later files?
  5. **The worker flagged:** an option inserted where absent is written quoted (`word: 'true'`). Decide whether that is a correctness defect for espanso (a string where a boolean is expected) or an acceptable, recorded D2u-consistent limit.
  6. Identity errors now cross the IPC boundary with their own i18n accessor — check EN/ES completeness and that comments claiming otherwise were corrected everywhere.
  7. Comments or notes claiming a guarantee the code does not give (this project's worst defect class).
- **Review file:** `docs/reviews/phase-3-10.md`.
- **Time budget:** 15 minutes.
