Reviewer: codex adversarial review

Codex adversarial review of the working tree at /Users/ccarpio/Developer/Utils/espansoConfig.
Codex verdict: needs-attention. 2 finding(s): 0 blocker(s), 2 should-fix.

Do not ship yet: nested insertions fail valid combined edits, and unsupported duplicate containers produce incorrect presence metadata. The 25 existing targeted tests passed; findings below are based on code tracing.

- [SHOULD-FIX] medium · confidence 0.98 — crates/espansoconfig-core/src/draft/plan.rs:1382 — Make nested insertions compose with match-level structural edits
    When a match ends with vars and its last variable ends with params, adding both a parameter and a missing label produces two insertions at the same byte offset. The audit accepts t…
    Fix: Avoid shared insertion offsets across nesting levels, and account for independently verified nested mapping changes in ancestor expectations…
- [SHOULD-FIX] medium · confidence 0.99 — crates/espansoconfig-core/src/model/variable.rs:214 — Preserve the first container occurrence even when its shape is unsupported
    For a variable containing `params: scalar` followed by `params: {echo: x}`, skip_shape does not claim the first key. The second occurrence therefore overwrites params_presence and…
    Fix: Track the first observed container key regardless of successful modeling. Keep its presence, projected entries and nested metadata aligned,…

Scope: the working tree only — anything already committed for this phase was outside it.

VERDICT: ship-with-fixes
BLOCKERS: none
SHOULD-FIX: crates/espansoconfig-core/src/draft/plan.rs:1382 — Make nested insertions compose with match-level structural edits — Avoid shared insertion offsets across nesting levels, and account for independently verified nested mapping changes in ancestor expectations…
SHOULD-FIX: crates/espansoconfig-core/src/model/variable.rs:214 — Preserve the first container occurrence even when its shape is unsupported — Track the first observed container key regardless of successful modeling. Keep its presence, projected entries and nested metadata aligned,…
NOT-VERIFIED: Add the failing composition and unsupported-first duplicate cases, fix their underlying paths, then rerun the targeted suites and required gates.
