# Review brief — step 4-7 (placeholder, reference and dependency analysis)

- **Repo:** /Users/ccarpio/Developer/Utils/espansoConfig (review the **uncommitted** working tree against `HEAD`).
- **Phase and goal:** Phase 4 step 4-7, defined in `docs/decisions/4-split-notes.md` §2 `### 4-7` (with the §3 rulings it relies on). A core parser for the supported `[[identifier]]` placeholder subset that keeps every character and reports malformed regions; one shared `{{reference}}` scanner on `REFERENCE_PATTERN` (now also used by validate rule 5); a dependency graph with explicit `depends_on` and inferred parameter-value edges kept distinguishable; usage counts; form sub-reference advisories; incomplete-analysis reasons; two new `SuspiciousButPermitted` findings `VariableDependencyCycle` and `DependencyHasNoDeclaration`, produced only for a variable operation that introduces or worsens the condition and bound to the candidate revision. No production UI caller.
- **Acceptance (from the split notes):** repeated and malformed placeholders; `{{f.field}}` sub-references; explicit and inferred cycles; regex captures; duplicate names; `inject_vars` spellings; imports keep the scope open (no false closed-scope claim); an unrelated save acquires no new acknowledgement; consent for one candidate cannot be spent on changed text; shorthand `form:` stays out of `rendered_content`.
- **Changed files:** new `crates/espansoconfig-core/src/analysis/{mod,placeholder,reference,dependency,findings}.rs`, `crates/espansoconfig-core/tests/dependency_analysis.rs`, `docs/decisions/4-7-notes.md`; modified `crates/espansoconfig-core/src/{lib.rs,validate/mod.rs,persist/save.rs}`, `crates/espansoconfig-core/tests/validate_semantics.rs`, `src-tauri/src/{wire_contract,dictionary_contract}.rs`, `src/lib/ipc/types.ts`, `src/lib/i18n/{en,es}.json`. (`PROGRESS.json` is a workflow record — ignore.)
- **Verification run (all exit 0):** `cargo test --workspace -- --test-threads=1` 1697 passed / 0 failed; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo fmt --check`; `npm run check` 487 files 0 errors 0 warnings; `npm test` 4086 passed; `npm run build` 214 modules; `cargo tree -p espansoconfig-core` has no tauri. Failing-first by 8 mutations of the new checks (`/private/tmp/4-7/mutation-*.txt`).
- **Risks to probe:**
  1. The introduce-or-worsen rule: can an unrelated save (no variable operation) acquire a new acknowledgement? Can a variable edit that does not change a cycle still produce one? "Worsens" compares cycle members by name (a rename of a member reports a new cycle) — is that sound?
  2. Consent binding: is each finding content-addressed to the candidate revision so an acknowledgement cannot be spent on changed text? Does the exact-multiset acknowledgement match in `persist/save.rs` still hold?
  3. Refactor of validate rule 5 onto the shared scanner: any behaviour change to existing findings (false positives/negatives), especially around imports keeping scope open and the synthesised `form1` (the worker recorded an open item: rule 5 may report `{{form1.x}}` in a parameter as undeclared while the analysis resolves it).
  4. Inferred edges only when injection is *certainly* enabled — check the `inject_vars` spellings handling honours D2u (source text, never an inferred YAML type beyond what the notes state).
  5. Placeholder parser: every character kept, malformed regions reported, UTF-8 byte offsets correct.
  6. Project rules: core never depends on tauri; every new public item documented; EN+ES strings exist for both new codes; wire contract updated consistently in `types.ts`; no real-config content quoted anywhere; claims in `4-7-notes.md` that the code does not actually guarantee (this project's worst defect class).
- **Review file:** `docs/reviews/4-7.md`.
- **Time budget:** 15 minutes.
