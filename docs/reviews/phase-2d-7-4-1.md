Reviewer: codex adversarial review

Codex adversarial review of the working tree at /Users/ccarpio/Developer/Utils/espansoConfig.
Codex verdict: needs-attention. 3 finding(s): 1 blocker(s), 2 should-fix.

Do not ship yet: concurrent launches can share identity, keyboard input is not confined to the named window, and cleanup can destroy clipboard contents. All 40 fixture checks and 21 recorded hashes passed; no app was launched.

- [BLOCKER] high · confidence 1 — /private/tmp/espansoconfig-harness-2d-6-6c-2/launch-7.sh:270 — Reserve launch names atomically
    Two concurrent invocations using the same new name can both pass these existence and ledger checks. Neither reserves the name; build_tree later uses mkdir -p, which accepts the sha…
    Fix: Atomically reserve the launch directory with mkdir without -p and require success before staging anything. Preserve failed reservations and…
- [SHOULD-FIX] medium · confidence 0.99 — /private/tmp/espansoconfig-harness-2d-6-6c-2/tools/post-input.swift:277 — Verify keyboard focus belongs to the named window
    For keyboard actions, both window checks validate ownership, visibility and geometry but never keyboard focus. If the process owns visible window A while window B has focus, --wind…
    Fix: Before keyboard posting, require the process's focused window to match the requested window id; refuse unavailable or ambiguous focus. Remov…
- [SHOULD-FIX] medium · confidence 1 — /private/tmp/espansoconfig-harness-2d-6-6c-2/launch-7.sh:627 — Avoid unconditional text-only clipboard restoration
    Every completed launch executes pbcopy, even when its plan never touches the clipboard. CLIP_BEFORE preserves only pbpaste output, so an existing image or other non-text payload is…
    Fix: Leave the clipboard untouched for plans without copy actions. For copy tests, preserve all pasteboard representations and restore only when…

Scope: the working tree only — anything already committed for this phase was outside it.

VERDICT: ship-with-fixes
BLOCKERS: /private/tmp/espansoconfig-harness-2d-6-6c-2/launch-7.sh:270 — Reserve launch names atomically — Atomically reserve the launch directory with mkdir without -p and require success before staging anything. Preserve failed reservations and…
SHOULD-FIX: /private/tmp/espansoconfig-harness-2d-6-6c-2/tools/post-input.swift:277 — Verify keyboard focus belongs to the named window — Before keyboard posting, require the process's focused window to match the requested window id; refuse unavailable or ambiguous focus. Remov…
SHOULD-FIX: /private/tmp/espansoconfig-harness-2d-6-6c-2/launch-7.sh:627 — Avoid unconditional text-only clipboard restoration — Leave the clipboard untouched for plans without copy actions. For copy tests, preserve all pasteboard representations and restore only when…
NOT-VERIFIED: Fix the three harness/tool issues and update the corresponding guarantees in the notes.
NOT-VERIFIED: Verify concurrent name refusal and clipboard preservation; keep real-window input validation in the authorized owner-present phase.
