Reviewer: codex adversarial review

Codex adversarial review of the working tree at /Users/ccarpio/Developer/Utils/espansoConfig.
Codex verdict: needs-attention. 4 finding(s): 3 blocker(s), 1 should-fix.

Do not ship: 3 BLOCKERS and 1 SHOULD-FIX. In-memory probes reproduced submission bypass, lost replay deliveries, and reapply dropping a retained restriction across all three operation sessions.

- [BLOCKER] high · confidence 1 — src/lib/browser/matchDeletion.ts:804 — [BLOCKER] Submission gates check a session displaced during projection reads
    A projected.document getter can call BrowserState.observeExternalChange, whose registered receiver replaces the installed session with an external conflict. confirmDelete then chec…
    Fix: After evaluating caller-controlled projection operands, read the installed session through a live reader or validate an authoritative sessio…
- [BLOCKER] high · confidence 1 — src/lib/browser/matchDuplication.ts:1196 — [BLOCKER] Deliveries arriving during replay are silently discarded
    Reproduced through BrowserState on all three *CouldNotBeSent paths: the live session holds retained(A), raised(A); during settlement replay, A.document's getter publishes B. The re…
    Fix: Serialize settlement replay with receiver delivery. Install settlement into the live session and append reentrant deliveries to the same con…
- [BLOCKER] high · confidence 1 — src/lib/browser/matchMove.ts:2587 — [BLOCKER] Reapply can adopt and discard a restriction delivered during evidence lookup
    With external conflict A standing, a correspondence row's exact getter starts another surface's pending raw write and publishes observation B. BrowserState delivers retained(B), up…
    Fix: Recheck the live session's uncertainty, retained wait, and identity after correspondence and projection reads, immediately before adoption.…
- [SHOULD-FIX] medium · confidence 1 — docs/decisions/2d-6-4-notes.md:90 — [SHOULD-FIX] The claimed refusal-before-evidence ordering is false
    The record says blocked reapplies refuse before reading any evidence. Each implementation instead calls enterReapply first (matchDeletion.ts:1560, matchDuplication.ts:1861, matchMo…
    Fix: Move existing uncertainty and retained-wait checks ahead of enterReapply on all three surfaces. Pin zero evidence reads for already-blocked…

Scope: the working tree only — anything already committed for this phase was outside it.

VERDICT: ship-with-fixes
BLOCKERS: src/lib/browser/matchDeletion.ts:804 — [BLOCKER] Submission gates check a session displaced during projection reads — After evaluating caller-controlled projection operands, read the installed session through a live reader or validate an authoritative sessio…
BLOCKERS: src/lib/browser/matchDuplication.ts:1196 — [BLOCKER] Deliveries arriving during replay are silently discarded — Serialize settlement replay with receiver delivery. Install settlement into the live session and append reentrant deliveries to the same con…
BLOCKERS: src/lib/browser/matchMove.ts:2587 — [BLOCKER] Reapply can adopt and discard a restriction delivered during evidence lookup — Recheck the live session's uncertainty, retained wait, and identity after correspondence and projection reads, immediately before adoption.…
SHOULD-FIX: docs/decisions/2d-6-4-notes.md:90 — [SHOULD-FIX] The claimed refusal-before-evidence ordering is false — Move existing uncertainty and retained-wait checks ahead of enterReapply on all three surfaces. Pin zero evidence reads for already-blocked…
NOT-VERIFIED: Fix the three interleavings and add regressions, then rerun check, tests, and build.
NOT-VERIFIED: Checked and did not hold: pre-await session capture in current components; stale/ambiguous correspondence acceptance; subject/anchor table switching; cross-sequ…
NOT-VERIFIED: Validation used read-only, in-memory probes against the actual TypeScript/Svelte modules and BrowserState. No files were modified; the full suite was not rerun.
NOT-VERIFIED: VERDICT: do-not-ship BLOCKERS: 3 SHOULD-FIX: 1
