# Review brief — step 4-2 (B1 reproduction and bounded repair)

- **Repo:** `/Users/ccarpio/Developer/Utils/espansoConfig` (macOS Tauri v2 + Svelte 5 + Rust; espanso YAML
  editor that must never reformat untouched bytes). Rules: `CLAUDE.md`.
- **Phase and goal:** step 4-2 of `docs/decisions/4-split-notes.md` §2 (`### 4-2`). Reproduce
  `docs/decisions/3-6-3-notes.md` §4 item 1 — a single-match editor draft holding a list-item addition
  (`triggers`, `search_terms`) never drew the external-change panel — failing-first, distinguishing missing
  delivery / model state / rendering, and repair only the reproduced cause.
- **Changed files (uncommitted tree):**
  - `src/lib/components/MatchEditor.svelte` — the retained-draft `{#each}` keyed by `index` instead of
    `field.label` (the claimed cause: duplicate labels per list item → Svelte `each_key_duplicate` aborting
    the flush).
  - `src/lib/components/DetailPane.test.ts` — new mounted (jsdom) B1 suite, 8 cases.
  - `src/lib/browser/matchEditor.test.ts` — a model-layer case.
  - `docs/decisions/4-2-notes.md` — the step's record.
  - `PROGRESS.json` — workflow record only; not under review.
- **Verification run (orchestrator, all exit 0):** `npm test` 4086 passed (88 files); `npm run check` 487
  files, 0/0; `npm run build` 214 modules; bundle oracle server-only markers absent, client-only present.
  No Rust file changed, so the Rust gates were not re-run. Failing-first evidence:
  `/private/tmp/4-2/failing-first-final-tests-unrepaired.txt` (6 failed, 2 passed, each with
  `each_key_duplicate`).
- **Risks to probe:**
  1. Is the diagnosis right — is `each_key_duplicate` really what B1 was, and do the tests exercise the
     real delivery path (envelope → workspace → session → DetailPane → MatchEditor) rather than a shortcut?
  2. Does keying by index introduce any stale-row / wrong-reuse rendering problem in that panel (the
     panel is read-only while up)?
  3. Are there other `{#each ... (field.label)}` or label-keyed blocks over the same model lists
     (save-conflict panel, `MatchCreator.svelte`, bulk) that the same defect reaches, and does the notes
     file describe them truthfully?
  4. Does `4-2-notes.md` claim anything the code or tests do not give (mounted evidence is not a window
     reading — ruling 29; the visible counterpart is owed to 4-13)?
  5. CLAUDE.md conventions on touched code (comments claiming guarantees, JSDoc, closing-bracket comments).
- **Review file:** `docs/reviews/4-2.md`.
- **Time budget:** 15 minutes.
