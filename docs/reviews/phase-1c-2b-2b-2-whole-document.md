1. [en.json:81](/Users/ccarpio/Developer/espansoConfig/src/lib/i18n/en.json:81), [es.json:81](/Users/ccarpio/Developer/espansoConfig/src/lib/i18n/es.json:81), [SourceText.svelte:39](/Users/ccarpio/Developer/espansoConfig/src/lib/components/SourceText.svelte:39) — The prose-substitution argument is defensible: the visible marker identifies the character and its tooltip explicitly explains the substitution, although a `title` tooltip is not always discoverable. It does not cover line endings: LF and CRLF become identical, unlabeled `<br>` elements, despite the caption claiming characters are shown “as the file writes them.” Reword the user-facing string in both languages—for example, “View of the whole file: invisible characters are named, and all line endings are shown as line breaks.” Merely documenting the exception would leave the screen’s claim false. **BLOCKING**

2. [rawDocument.test.ts:48](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/rawDocument.test.ts:48) — The pure-state tests accurately establish `has none`, `takes the sidebar’s file`, `prefers`, `falls back`, `no longer listed`, `loading`, `carries text untouched`, empty/refused distinction, and whitespace-only handling. “Takes a file … even when it holds no snippets” only tests a summary classified as a profile; “carries the failure whole, so four refusals do not share one sentence” tests one failure and no sentences. Rename those claims to what the bodies establish. **FOLLOW-UP**

3. [rawDocument.test.ts:137](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/rawDocument.test.ts:137) — “Has every one … in both languages” is supported as a nonempty-key check. “Says two different things” proves only string inequality, while “labels the toggle by what it will do” proves only that the strings contain no interpolation placeholder; either semantic claim could be false while its test passed. **FOLLOW-UP**

4. [rawDocument.test.ts:187](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/rawDocument.test.ts:187) — All four source-scan names overpromise UI behavior: “draws a whole document,” “claims … inside the text arm,” “draws the refusal,” and “draws the toggle” inspect substrings, not rendered DOM. The bodies establish source wiring or placement only; rename them accordingly, especially the three names explicitly promising that something “draws.” **FOLLOW-UP**

5. [workspace.test.ts:872](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.test.ts:872) — The target, command-count, arm-transition, reread, close, and race tests otherwise match their state-level names. Three overreach: “says a file … is refused” and “reports … to the screen” establish state, not rendering; “tells a file of no characters apart from one it could not read” supplies only an empty-file answer, so it still passes if unreadable files are also classified as empty. **FOLLOW-UP**

6. [workspace.svelte.ts:448](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:448), [workspace.svelte.ts:724](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:724) — `clearSelection()` and the clearing arms of `applyRepair()` can move an “All” target to `null` without invalidating its pending text read. If that answer later lands and the same file is selected again, `readFileText()` returns early because its identity is still held, reusing the old snapshot; while targetless, `fileTextShown` also remains true with neither viewer nor toggle visible. Clear/invalidate the file-text request whenever these paths remove the target, and add the missing race test. **BLOCKING**

7. [workspace.svelte.ts:415](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:415) — Apart from the target-clearing path above, the generation check and identity-guarded getter prevent a stale answer from appearing beneath another file’s name; typed command failures also settle the loading arm. No additional stuck-loading path was found under the `BrowserCommands` contract. **FOLLOW-UP**

8. [workspace.svelte.ts:420](/Users/ccarpio/Developer/espansoConfig/src/lib/browser/workspace.svelte.ts:420) — Phase 2 cannot refresh the currently displayed file after a successful write: identity-based deduplication suppresses another read, and the deliberately removed `force` path leaves closing/reopening as the only refresh mechanism. Add an explicit post-write invalidation/refresh operation, and do not treat `RawDocumentText.text`—which carries no revision—as sufficient authority for a write. **FOLLOW-UP**

Codex session ID: 019fba42-0a7d-77e1-a34d-05854f6627ea
Resume in Codex: codex resume 019fba42-0a7d-77e1-a34d-05854f6627ea
