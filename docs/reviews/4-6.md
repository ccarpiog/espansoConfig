Reviewer: codex adversarial review

Codex adversarial review of the working tree at /Users/ccarpio/Developer/Utils/espansoConfig.
Codex verdict: needs-attention. 2 finding(s): 0 blocker(s), 2 should-fix.

Do not ship yet: form-setting edits can change YAML types, and mapping insertion skips nested key validation. Existing targeted binaries passed 33 tests, but do not cover these cases. No files were modified.

- [SHOULD-FIX] medium · confidence 0.99 — crates/espansoconfig-core/src/draft/form_plan.rs:527 — Preserve plain-source semantics when editing existing form settings
    The newly exposed verbose option-edit path delegates to plan_open_mapping, which emits ScalarEdit::new through plan_scalar. Consequently, setting an existing multiline: true to "fa…
    Fix: Recognize both setting keys when planning existing options, validate their source text before no-op comparison, and emit ScalarEdit::plain_s…
- [SHOULD-FIX] medium · confidence 0.98 — crates/espansoconfig-core/src/patch/edit.rs:5356 — Validate nested keys in mapping-valued insertion groups
    request.mappings goes directly to rendering; check_inserted_fields/check_nested_keys is called only for request.items. A direct core caller can supply a definition containing both…
    Fix: Apply recursive key validation to every mapping-valued group entry before rendering, preserving legal empty mappings. Add direct engine test…

Scope: the working tree only — anything already committed for this phase was outside it.

VERDICT: ship-with-fixes
BLOCKERS: none
SHOULD-FIX: crates/espansoconfig-core/src/draft/form_plan.rs:527 — Preserve plain-source semantics when editing existing form settings — Recognize both setting keys when planning existing options, validate their source text before no-op comparison, and emit ScalarEdit::plain_s…
SHOULD-FIX: crates/espansoconfig-core/src/patch/edit.rs:5356 — Validate nested keys in mapping-valued insertion groups — Apply recursive key validation to every mapping-valued group entry before rendering, preserving legal empty mappings. Add direct engine test…
NOT-VERIFIED: Add the missing regressions, fix both paths, and rebuild and rerun the core tests.
