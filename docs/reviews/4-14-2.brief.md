# Review brief — step 4-14-2 (existing variables' parameters)

- **Repo:** /Users/ccarpio/Developer/Utils/espansoConfig (review the **uncommitted** working tree against HEAD `ff665ba`).
- **Phase and goal:** step 4-14-2 of Phase 4 (spec: `docs/decisions/4-split-notes.md` §2, the 2026-09-26
  addendum under `### 4-14`). Drafting an existing variable's `params` values, `depends_on` and list items
  in the match editor through the shared undo/conflict/recovery/save lifecycle; and, in Rust, an edited
  existing `offset`/`trim`/`debug` in a variable's `params` written as validated plain source through
  `plan_open_mapping` rather than as a quoted logical string (failing-first test). Origin:
  `docs/decisions/4-9-notes.md` §5 item 4 and §8 finding 1; `docs/decisions/4-6-notes.md`.
- **Record:** `docs/decisions/4-14-2-notes.md` (deviations, open items, failing-first evidence).
- **Changed files:** `crates/espansoconfig-core/src/draft/{plan.rs,form_plan.rs,error.rs}`,
  `crates/espansoconfig-core/tests/draft_plan.rs`; new `src/lib/browser/variableParams.ts` (+ test);
  `src/lib/browser/{variableEditor,formEditor,matchEditor,variableGroup,variableKinds,fixtures}.ts`,
  `src/lib/components/{VariableGroup.svelte,MatchEditorVariables.test.ts}`,
  `src/lib/i18n/{en.json,es.json,index.ts}`, `src/lib/ipc/types.ts`. (`PROGRESS.json` is a workflow record.)
- **Verification run by the orchestrator, all exit 0:** `cargo test --workspace -- --test-threads=1`
  (1738 passed), clippy `-D warnings`, `cargo fmt --check`, `npm run check` (506 files, 0/0),
  `npm test` (4424), `npm run build` (225 modules), bundle oracle correct, core has no tauri.
- **Risks to probe:** (1) 4-6's `plan_typed_settings` pass was deleted and folded into
  `plan_open_mapping` — does every form-setting case it covered still write plain source and refuse
  non-plain text by name, and did the error-order change break any caller's expectation? (2) byte
  preservation outside the edited span for params/list-item/depends_on edits; (3) `\r` refusal at load,
  edit and send (CLAUDE.md §6), and check-and-spend atomicity (property reads between check and spend);
  (4) the draft merging a verbose form's `layout` with params edits into one `VariableDraft` — can one
  overwrite the other? (5) D2u — values shown as source text, no inferred type; (6) i18n: no hardcoded
  user-facing strings, EN/ES parity; (7) records claiming guarantees the code does not give.
- **Project rules:** `CLAUDE.md` (sections 2, 3, 5, 6).
- **Review file:** `docs/reviews/4-14-2.md`.
- **Time budget:** 15 minutes.
