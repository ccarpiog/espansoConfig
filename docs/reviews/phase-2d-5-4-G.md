Reviewer: codex adversarial review

Codex adversarial review of the working tree at /Users/ccarpio/Developer/Utils/espansoConfig.
Codex verdict: needs-attention. 4 finding(s): 0 blocker(s), 4 should-fix.

ship-with-fixes — 2 blockers and 2 SHOULD-FIX findings. The removal fix loses an existing guard, and recovery can reopen after disposal. Runtime counterexamples use injected boundaries; production reachability is not claimed. No files modified or tests/builds run.

- [SHOULD-FIX] medium · confidence 1 — src/lib/browser/observationTransitions.ts:1493 — BLOCKER: Restore the ownership check after removeDocument
    Source correctness regression. After isNewest succeeds, the injected removeDocument can admit a newer observation before noteDocumentStatus writes 'removed'. For example, remove Na…
    Fix: Keep the guard before removal, then call noteWhileOurs for the status write. Add a Named-removal case that admits a newer observation inside…
- [SHOULD-FIX] medium · confidence 0.99 — src/lib/browser/reconciliationCoordinator.ts:1041 — BLOCKER: Check lifecycle before recovery performs the reopen
    Source correctness defect in the complementary branch newly declared safe here. Deliver a batch with discarded=1 and make host.openWriteSurfaces call coordinator.dispose() before r…
    Fix: Pass the captured lifecycle into recovery and compare it after the registry read and its length access, before resetting state or invoking r…
- [SHOULD-FIX] medium · confidence 1 — src/lib/browser/reconciliationCoordinator.ts:988 — SHOULD-FIX: The snapshot does not make the fifth read inert
    Source-comment and record defect, not a missing runtime fence. runOneDrain copies observations by reference at line 1348. Consequently observations.length at line 983 still invokes…
    Fix: Limit the inert-read claim to the four top-level data properties. Explicitly retain observations.length as a live caller-controlled read pro…
- [SHOULD-FIX] medium · confidence 0.99 — docs/decisions/2d-5-2a-A-notes.md:210 — SHOULD-FIX: The identity sweep leaves the unrelated-file rationale standing
    Record-only defect. This passage still concludes that a surviving registration causes refusal over an unrelated file; lines 169–172 derive that conclusion by asserting that its Doc…
    Fix: Add an explicit correction to §3's derivation and unrelated-file conclusion, preserving historical quotations while distinguishing path iden…

Scope: the working tree only — anything already committed for this phase was outside it.

VERDICT: ship-with-fixes
BLOCKERS: none
SHOULD-FIX: src/lib/browser/observationTransitions.ts:1493 — BLOCKER: Restore the ownership check after removeDocument — Keep the guard before removal, then call noteWhileOurs for the status write. Add a Named-removal case that admits a newer observation inside…
SHOULD-FIX: src/lib/browser/reconciliationCoordinator.ts:1041 — BLOCKER: Check lifecycle before recovery performs the reopen — Pass the captured lifecycle into recovery and compare it after the registry read and its length access, before resetting state or invoking r…
SHOULD-FIX: src/lib/browser/reconciliationCoordinator.ts:988 — SHOULD-FIX: The snapshot does not make the fifth read inert — Limit the inert-read claim to the four top-level data properties. Explicitly retain observations.length as a live caller-controlled read pro…
SHOULD-FIX: docs/decisions/2d-5-2a-A-notes.md:210 — SHOULD-FIX: The identity sweep leaves the unrelated-file rationale standing — Add an explicit correction to §3's derivation and unrelated-file conclusion, preserving historical quotations while distinguishing path iden…
NOT-VERIFIED: Fix both source correctness blockers and the two false guarantees.
NOT-VERIFIED: Commission a review scoped to the resulting source changes under CLAUDE.md §7.1.
